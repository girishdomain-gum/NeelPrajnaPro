# S5-1 · session log · 2026-07-25 · developer (claude-code)
Instruction: ARCH-005 (Sprint 5 — Battery I: engine + splits + selftest)
Branch: claude/arch-005-t0-execution-7549cc · Pushed through: (this session, then merge)

DONE (ARCH-005 complete — T0 + all four scope items + DEVQs):
- T0 — appended GO-S4 note 01KYBX4SWX0DJXSV59526CZHD6 (parent GO-S3 note
  01KYB5ARJPK3YK0AKCE9FP7DAH; decision GO, both Owner phrases, key S4 record ids).
  scripts/note_go_s4.py, idempotent. Journal 25 → 26, chain GREEN.
- §2 seeds — kernel/protocol/seeds.py: seeds.for_run(hypothesis_ref, window_ref)
  → deterministic 63-bit int (documented recipe: sha256 of canonical_bytes of the
  two anchors). Reproducible-by-hand test + input-sensitivity + validation.
- §3 splits — kernel/protocol/splits.py: anchored walk-forward; n_folds+1 blocks,
  anchored expanding train with an embargo boundary gap. Boundary matrix,
  no-overlap + in-window + anchored + determinism properties, embargo-at-edges,
  validation. Convention recorded in DEVQ-011.
- §1 engine — trading/simulator/engine.py + fills.py: audited event engine.
  No look-ahead by construction (next-open entry; exits read no future bar; trade
  dropped if it can't close); gross AND net via DEVQ-008 cost model; the Simulator
  TYPE (kernel/battery/simulator.py) — screener rejected by marker+simulate().
  Acceptance: hand micro-scenario gross +4.00 / net +2.59 to the cent;
  no-look-ahead incremental-feed property; byte-identical determinism twice + across
  a process restart (subprocess). Fill rule recorded in DEVQ-012.
- §4 selftest — kernel/battery/selftest.py: three seeded synthetic suites (planted
  edge / pure noise / small-n) + tri-state classifier (one-sided t-test + seeded
  bootstrap CI groundwork). Engine INJECTED (firewall stays clean); tri-state
  correct on all three wired to the real engine; no verdict written (AST-audited).
  Calibration recorded in DEVQ-013.
- Deps entered: scipy, statsmodels, arch (pyproject + uv.lock). gen_state run.

IN-PROGRESS: — (session closing after merge + push)
BLOCKED-ON: -   NEXT: Architect IVF S5 (determinism cross-run byte compare;
fill/cost spot recompute; split+embargo independent recomputation; selftest
tri-state audit) + Drill S5 (planted look-ahead fill + embargo violation, run
BEFORE real check) + HC (sampled trades on the MT5 chart). Owner HC + Go/No-Go →
GO-S5. Ratify DEVQ-011/012/013 (all non-blocking; code proceeds on the
recommendations).
Tests: 648 passed (+460 over the S4 baseline of 188), 1 warning (pre-existing
pandas_ta deprecation) · ruff clean · firewall GREEN · journal 26 records, chain
GREEN (one T0 append this session; no other journal writes).

DEVQs raised (all QUESTION, non-blocking, recording ratifiable choices — exactly
the three areas the ARCH-005 DoD predicted):
- DEVQ-011 embargo placement (contiguous boundary gap vs LdP purge-after-test).
- DEVQ-012 engine fill rule (fixed next-open + pessimistic intrabar stop-before-target).
- DEVQ-013 selftest calibration (decisive planted edge, MIN_N=30, alpha=0.05).

No writes outside permitted paths: qrf/** + tests/** + scripts/** (code),
pyproject/uv.lock (deps), 3 new DEVQs in inbox/OPEN, state via gen_state, this
session log, and the sanctioned completion-report append to ARCH-005. One journal
append (T0 GO-S4 note via RecordStore.append); datastore/bulk untouched.
