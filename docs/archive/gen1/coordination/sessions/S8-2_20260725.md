# S8-2 · session log · 2026-07-25 · developer (claude-code)
Instruction: ARCH-008 §4 completion — the DEVQ-021 micro-session (vendor
smc-toolkit, flip the §4 gate, run the reconciliation, append the ARCH-008
addendum). Branch: claude/devq-021-smc-toolkit-c6240a ·
Pushed through: **see PUSH below**.

CONTEXT: Booted per CLAUDE.md. `git fetch origin` OK; `origin/main` already merged
(Already up to date; branch off main at dd6986a). Read PROTOCOL, AI_PROJECT_STATE,
the latest session log (S8-1), and the DEVQ-021 ruling in inbox/CLOSED. Scope was a
single delegated micro-task, not a sprint. Worktree has no `.venv`; used the main
venv `F:/QRF/.venv/Scripts/python.exe` with `PYTHONPATH=<worktree>` so the editable
`qrf` resolves to worktree code (same convention as S8-1).

DONE (ARCH-008 §4 — DEVQ-021 discharged):
- **Vendored** `github.com/Louisjzhao/smc-toolkit` `core.py` + `__init__.py` at commit
  `812de852f0e0a6bf454720d0ea11ad5c7c64b4ef` into
  `tests/third_party/smc_toolkit_vendored/`, with the upstream MIT `LICENSE` alongside
  and a per-file provenance header (repo, commit, retrieval date, upstream sha256). The
  header records the sha256 of the pristine bytes BELOW a sentinel line; files were
  assembled byte-faithfully (LF-preserving) so both hashes reproduce the ruling's exactly
  (`core.py` = `056a9fdb…b92288f`, `__init__.py` = `82267473…8acf449`).
- **Provenance gate** — `tests/third_party/test_smc_toolkit_vendored_provenance.py`
  re-hashes the below-sentinel bytes and asserts == the recorded sha256s + MIT LICENSE +
  header contents. Offline, drift-proof (behaves like a pinned dep).
- **§4 gate flipped** — `tests/concepts/test_smc_cross_impl_s8.py` drops
  `pytest.importorskip` and imports the vendored `extract_fvg` plainly. Added a sample
  dataset (`_SAMPLE_BARS`, 13 bars → six genuine plain gaps of varying displacement) and
  ran the reconciliation vs our calibrated `smc.fvg`. `_theirs()` adapter maps the
  library's MultiIndex API to `{ts,direction,zone_hi,zone_lo}`, stamping `end_time` (the
  third/confirming bar = our knowability moment) and DELIBERATELY never reading the
  `mitigated` column (lookahead by construction).
- **Reconciliation (library-level IVF):** our detector first shown == the clean-room
  plain-gap rule (6=6). Against the vendored lib: **n_ours 6 / n_theirs 2 / matched 2 /
  only-ours 4 `[(5,+1),(8,+1),(9,+1),(13,−1)]` / only-theirs 0 / zone-mismatch 0.** The
  vendored lib is a strict SUBSET — same core gap rule, same knowability bar, identical
  zone bounds on the intersection; it differs ONLY by its two extra conditions (mid-bar
  close beyond bar-1's extreme + displacement > 2× expanding-mean body move). Every
  divergence traces to exactly one DIFFERENCE_MAP axis.
- **DIFFERENCE_MAP rewritten** for the vendored lib specifically (5 axes:
  pattern_and_knowability, zone_bounds, mid_close_condition, displacement_filter,
  mitigation_lookahead) and VERIFIED against the vendored source via
  `test_difference_map_matches_vendored_source` (each axis names a source token; also
  asserts the spurious `/100` and the future-scanning `mitigated` are genuinely present).
- **Independence boundary** (ruling item 3) — extended the kernel firewall
  (`tests/test_kernel_firewall.py`): a scanner over `qrf/**` asserts NO production module
  imports the vendored fixture, plus a planted-violation test. The vendored module is
  tests-only.
- **ruff** — excluded `tests/third_party/smc_toolkit_vendored` (byte-identical vendored
  source, must not be reformatted; same rationale as the `ivf` exclusion in NOTE-007).
- **ARCH-008 addendum** appended (sanctioned completion-report append) with the
  agreement/difference counts and the DoD delta. **gen_state** regenerated.

RESULT: Tests **786 passed** (was 777 passed + 1 skipped; the skipped external-library
leg is now a real passing test) · ruff clean · firewall GREEN (incl. new vendored
boundary) · journal **54 UNCHANGED** (test-only change, zero records written) · VIRGIN
untouched · gen_state regenerated.

PUSH: commit `3dd5213` (vendor + §4 + firewall + ruff + gen_state); addendum + this log
follow. All pushed to `origin/claude/devq-021-smc-toolkit-c6240a`; merged to `main` and
pushed per DoD (see final state below).

BLOCKED-ON: none. DEVQ-021 CLOSED and fully discharged; §4 complete.

NO WRITES OUTSIDE PERMITTED PATHS: new files under `tests/third_party/**` and
`tests/concepts/test_smc_cross_impl_s8.py`, `tests/test_kernel_firewall.py`,
`pyproject.toml` (ruff exclude) — all code/config paths this micro-task owns; the
sanctioned ARCH-008 completion-report addendum; `docs/coordination/sessions/` (this log);
`docs/handover/AI_PROJECT_STATE.md` via `gen_state.py` only. journal.jsonl NOT touched.

NEXT (Architect): fold §4 into the S8 review — the vendored second implementation is
available to the IVF as an independent FVG oracle (deps numpy/pandas/matplotlib only,
distinct from smartmoneyconcepts). Its `mitigated` column is lookahead and must stay out
of any knowability-respecting comparison; the reconciliation harness already enforces
detection-mask-only. DEVQ-021 can be marked discharged.
