# Session SNP-S1-03 · Sprint NP-S1 · 2026-07-30
Developer: claude-code, worktree `claude/np-adr-008-liquidity-sweep-7aa72b` (new worktree — see branch-discrepancy note below)

## Outcome (one line)
NP-ADR-008 confirmed ratified on mainline (T-039); resumed Execution Plan §4 at deliverable 1. **Deliverables 1–3 (partially) complete and verified**: detector + AC-1/AC-2, M5 tick ingestion (with a real broker-timezone bug caught and fixed before it reached the ledger), TRAINING window designated, both sealed H-007 registrations written. **Halted cleanly before deliverable 6 / the Battery run** on a newly-raised DEVQ (identity of "the 17" counted-only entries) — the ordering trap means the run cannot proceed until that's answered.

## What happened, in order

### 1. Ratification verification (did not proceed on the Owner's chat assertion alone)
The task briefing claimed "Mainline carries NP-ADR-008, ratified 2026-07-30," but a fresh `git fetch` showed mainline still at T-038, with the ADR's own status line reading "awaiting Owner rulings" and Execution Plan §0 unchanged from the pre-v1.1 state. Per this project's own repo-over-chat rule, I surfaced the discrepancy via `AskUserQuestion` rather than assume. The Owner confirmed it was a real sequencing gap (ratification not yet committed) and directed me to poll until T-039 landed. Polled at 5-minute intervals (per a later user request) until `git log origin/main` showed T-039 (`43fc355`/`c7854ed`); verified both required conditions directly in file content (`ops/NP-ADR-H07_definition_v1.1_draft_v2.0.md:1` header, Execution Plan `§0` text) before treating the sprint as unblocked.

### 2. Branch discrepancy
`ops/DEVELOPER_BOOT_NP-S1_RESUME.md` (delivered with T-039) instructs continuing on `claude/neelprajnapro-sprint-np-s1-a8171d` (PR #1's branch), but this session runs on `claude/np-adr-008-liquidity-sweep-7aa72b`, which was already current through T-039 while the named branch was 3 commits behind. Confirmed with the Owner (`git log` showing PR #1 already merged at `cdbb71c`, carrying the old branch's history into mainline) and was told to proceed here; the resume doc's branch pointer is stale and should be corrected in a future write window.

### 3. Recon (background workflow, 7 parallel agents)
Before writing code, mapped: the `Detector`/`EventFrame`/`CalibrationCase` contract (`qrf/kernel/instruments/base.py`); an existing detector + fixtures + test pattern (SMC); the hypothesis YAML schema and `HypothesisRegistry`/`TrialCountLedger`/`deflation.py::_trial_belongs_to_family`; `WindowLedger` designate/burn/check_available mechanics; `EvidenceBattery`'s real 8-step pipeline and confirmation that B1–B7 is bespoke-only terminology absent from the real Kernel entirely (no code reuse possible for the comparison report — a hand-built mapping is required, matching NP-ADR-008 M4); `BulkStore`/`RecordStore` and — critically — located and read the actual evidenced source (`F:\NeelPrajna\np_feature_service.py::build_pools_and_sweeps`, `detect_swings`) verbatim, including five non-obvious control-flow behaviors (boundary-inclusive reclose, member-window index reference point, sweep-check-before-new-pool ordering, monotone max_pen, duplicate-suppression-without-linkage) that a faithful port had to preserve exactly, not paraphrase.

### 4. Deliverable 1 — `qrf/trading/concepts/neelprajna/detector.py` (AC-1, AC-2)
Ported `detect_swings`/`build_pools_and_sweeps` verbatim (control flow) into the Kernel's `Detector` protocol, emitting `neelprajna.liquidity_sweep.pool_formed`/`.sweep`. Before writing the real fixtures, built a throwaway oracle (literal transcription of the evidenced source) in the scratchpad and used it to design and verify 6 planted/clean cases by hand-reasoned bar construction, including a case that specifically exercises the boundary-inclusive reclose ambiguity (reclose exactly at `reclose_window` bars still counts as a sweep, confirmed against the oracle) and an invalidation case (pool forms, never recloses — POOL_FORMED fires, no SWEEP). 14 tests, all passing; lint clean; kernel firewall unaffected (code lives under `qrf/trading/`, not `qrf/kernel/`). One pre-existing, unrelated test failure noted (`tests/adapters/test_mt5_csv.py::test_real_ivf_export_ingests_zero_flags` — a local data CSV absent from this fresh worktree; confirmed via `git status`/`git log` that I never touched that file).

### 5. M5 ingestion — `scripts/ingest_h07_m5_vantage.py`
Built ticks→M5-mid-bar aggregation from scratch (per NP-ADR-008 M7: the retired bespoke stack's own `bars_300s.parquet` may not be used evidentiarily) against the 60 Stage-2 tick parquet files at `F:\NeelPrajna\Validation\Stage2\parquet\`. **First run had a real bug**: treated `time_msc` as literal UTC, producing bars starting ~3h after the sealed window boundary. Cross-checked against `DEVQ-NP-002`'s closed reply ("the underlying tick capture runs to 17:32:59.764 **stored**" — broker time) and confirmed the tick file's own timestamps needed the same broker-EEST(UTC+3)→UTC correction already established for the trades export. Since the bad manifest+window records were local and uncommitted, reverted via `git checkout` (not a ledger amendment — nothing had relied on them yet) rather than leaving permanent clutter, then re-ran with the fix. Result: **16,029 M5 bars** — exactly matching the row count of the retired bespoke stack's own `bars_300s.parquet` (an independent cross-check my aggregation logic is right), observed ts range `[2026-04-20T22:05:00Z, 2026-07-10T14:35:00Z]` consistent with the sealed `[2026-04-20T22:00:00Z, 2026-07-10T14:33:00Z)` boundary once M5 close-time bucketing is accounted for.

### 6. Window designation
`WindowLedger.designate("xauusd_m5_vantage", ..., "TRAINING", ...)` over the exact sealed NP-ADR-008 interval (hard-coded, not derived from the ingest) — window `01KYSEDSM6K5ZKWK0XRCC4SVZ7`.

### 7. Deliverable 3 — the two sealed H-007 registrations
`configs/hypotheses/h007_np_liquidity_sweep_v1_1_{prediction,e2_existence}.yaml` + `scripts/register_h007_np_liquidity_sweep.py`. Both registered and verified against the sealed binding constants (`lineage`, `family`, `scope`, `cost_model_ref` all exact matches, confirmed by direct journal query after the run). **Found and fixed a second bug before it shipped**: my first draft of the registration script checked instrument idempotency via `InstrumentRegistry.get()`, which only reflects that *instance's* in-memory index — `register()` itself is not idempotent and would have appended a duplicate `instrument_registered` record on every re-run. Found the repo's own convention (`scripts/bootstrap_smc_s4.py`: query `store.query(record_type="instrument_registered")` directly) and matched it; verified by re-running the script and confirming `+0` records the second time.

Along the way, discovered and documented (`docs/coordination/notes/NOTE-NP-001_...md`) a **new divergence** beyond NP-ADR-008's M1–M7: the real audited `EventEngine` supports only a single fixed `stop_offset`/`target_offset` per hypothesis, not NP-ADR-008 §3's per-trade-variable stop (penetration extreme ±10 ticks, 1.5R target). Registered the prediction claim with no stop/target (pure time-stop at `hold_bars=12`, matching the evidenced 1h time stop exactly) rather than fabricate a fixed numeric stand-in — following the exact precedent already in this Kernel (`h001_fvg_follow_through.yaml`'s own `stop_offset: null`/"naive, no stop/target"). Did not raise a DEVQ for this specific choice (a hard engine-capability constraint with a well-precedented fallback, not a judgment call among several valid readings) but flagged it clearly for deliverable 5's comparison report, since it's a genuinely new AC-4-relevant finding.

### 8. DEVQ-NP-003 raised — halted before deliverable 6
While preparing deliverable 6 (the 17 counted-only entries), found no discoverable roster of "the 17" anywhere in the repository — and the Execution Plan's own §7 text says the Owner rules on "the 17" (subset, priority, n-floors) **at NP-S3, informed by NP-S1's own comparison report**, which doesn't exist yet. This is the same concern my predecessor session raised and the Architect endorsed holding (`DEVQ-NP-001`'s "How this blocks" §5) — NP-ADR-008 ratified the *count* (19 trials) but not the *identities*. Raised `docs/coordination/inbox/OPEN/DEVQ-NP-003_the_17_counted_only_identity.md` and stopped that line of work. Per the resume doc's own "ordering trap" (all 19 trials must be registered before `deflate_family` sees them at judgment, or the effective α comes out ~10× too loose), **the Battery run cannot proceed until this is answered** — so deliverables 4, 5, and the AC-4 mapping seal (naturally sequenced immediately before the run) are held too, not attempted.

## State for the next session
- **Unblocked and done:** deliverable 1 (detector, AC-1/AC-2), the M5 ingestion + window designation, deliverable 3 (both H-007 registrations).
- **Blocked on `DEVQ-NP-003`:** deliverable 6 (17 counted-only entries), and transitively deliverable 4 (Battery run), the AC-4 mapping seal, and deliverable 5 (comparison report).
- Two findings on record for the Architect: `NOTE-NP-001` (engine cannot express the evidenced variable stop/target — new AC-4-relevant divergence) and `DEVQ-NP-003` (the 17's identity — a forward-reference gap between NP-S1 deliverable 6 and NP-S3's own ruling text).
- One stale pointer worth a future write-window correction: `ops/DEVELOPER_BOOT_NP-S1_RESUME.md`'s branch instruction (names the old, now-behind PR #1 branch instead of this one).
- No edits to any normative document, `ivf/**`, or ledger internals beyond the sanctioned `RecordStore.append`-mediated writes (instrument/calibration/hypothesis/trial_count/window/bulk_manifest records, all via the real API).

## Definition of Done status
Partial, correctly gated: deliverables 1 and 3 meet their own acceptance criteria (AC-1, AC-2 verified by passing tests); deliverables 4–6, AC-3, AC-4, AC-5, AC-6 not attempted — blocked by DEVQ-NP-003, not skipped. `scripts/gen_state.py` not run (this is the gen2 estate; §0 of the Execution Plan is its own handover, matching my predecessor's same finding). This log written; committing and pushing now.
