# DEVQ-NP-004 · BLOCKER · Sprint NP-S1 · 2026-07-30
Author: developer (claude-code), worktree `claude/np-adr-008-liquidity-sweep-7aa72b`
Refs: NP-ADR-008 §2 (provenance chain) and §2.1 (non-equivalence statements); `ops/DEVQ-01_NP-S1.md` Addendum A (the "T3 read directly" finding this corrects); `F:\NeelPrajna\repo\Gates\Triggers\T3_SweepFVGGate.mqh` (header comment, read in full); `F:\NeelPrajna\knowledge_base\kb.json` (H-07, H-08 records); Constitution P5 (append-only), P9 (verification is layered), P10 (tripwires bind their authors).
Tag: architecture-conflict (provenance misattribution) — raised at the Owner's direction after their pointer to `kb.json` surfaced this while resolving DEVQ-NP-003.

## Summary (one line)
`T3_SweepFVGGate.mqh`'s own header states, in its own words, that it **"was Gate 8"** — but NP-ADR-008 §2 and the entire DEVQ-01 evidence chain attribute it to **H-07 (Gate 7)**. `kb.json`'s own roster lists H-07's gate as `LiquiditySweepGate.mqh` (Gate 7) — a file T3's header says was **deleted** during T3's v2.0 merge, its pool engine absorbed into T3 as one of two optional "anchor sources." §5 v1.0 is very likely H-08's frozen mechanical definition, not H-07's, and needs a correction — not an edit, an append (P5) — before any further registration cites it.

## What I verified directly (high confidence)
1. **`kb.json` (`F:\NeelPrajna\knowledge_base\kb.json`, read in full)**: H-07 = *"Equal-high/low sweep + reclose reverses (stop-hunt)"*, `lineage: "#002 Gate 7"`, `executable_definition: "LiquiditySweepGate.mqh -> np_feature_service.py"`, `status: rejected`, with an evidence trail (`stage3_feature_service`, `probability_engine_v0` FAIL citing `Stage4_EvidenceReport_H07.json`, `cost_threshold_analysis`) that is **unambiguously the same 324-trade population** this sprint has been calling H-07 all along — that identification is not in question.
   H-08 = *"1H sweep -> MSS -> FVG tap sequence (NY open) has edge"*, `lineage: "#002 Gate 8"`, `executable_definition: "SweepFVG1HGate.mqh"`, `status: candidate` (never evaluated, `maturity: M1`).
2. **Neither `LiquiditySweepGate.mqh` nor `SweepFVG1HGate.mqh` exists anywhere in the `F:\NeelPrajna\repo` git history** (`git log --all --diff-filter=A --name-only` for both, zero hits; filesystem search of the whole tree, zero hits). The only real file in this naming family is `Gates/Triggers/T3_SweepFVGGate.mqh`, present since the repo's first commit (`92a6ac4`, "Phase 0: v3.16.4 baseline") — i.e. `kb.json`'s `executable_definition` strings are descriptive/aspirational labels from its own catalog, not literal current filenames.
3. **`T3_SweepFVGGate.mqh`'s own header (read in full, quoted verbatim)**:
   > `TRIGGER T3 — 1H Liquidity Sweep + FVG (was Gate 8) (NY Open Playbook)`
   > `... v2.0 CHANGES: • MERGED POOL ENGINE — the old Gate 7 (LiquiditySweepGate.mqh) EQH/EQL pool engine is absorbed here as an alternative sweep-anchor source. InpT3_AnchorSource selects: T3_ANCHOR_CANDLE ... T3_ANCHOR_POOL ... T3_ANCHOR_BOTH ... LiquiditySweepGate.mqh is deleted — no G7_*/EG_G7_* symbols exist anymore.`
   The file's own 5-stage doctrine (ANCHOR → SWEEP → **MSS** → **FVG** → **TAP**, "NY Open Playbook") matches H-08's hypothesis text ("1H sweep -> MSS -> FVG tap sequence (NY open)") word-for-word better than it matches H-07's ("equal-high/low sweep + reclose reverses" — no MSS, no FVG, no NY-open, no 1H mentioned in H-07's own hypothesis text).
4. **The MSS stage is universal in T3, not conditional on anchor source.** Both `T3_ANCHOR_CANDLE` and `T3_ANCHOR_POOL` still route through the same mandatory MSS → FVG → TAP pipeline (the header's "CORE DOCTRINE" section, stages 1–5, is described once, applying to both anchor sources). So T3's pool-anchor mode is **not** a resurrection of the original (simpler) Gate 7 — it is Gate 7's pool-formation math grafted onto Gate 8's still-mandatory MSS/FVG/TAP machine.
5. `ops/DEVQ-01_NP-S1.md` Addendum A's own claim — *"the verifying session read `T3_SweepFVGGate.mqh` directly and confirmed that §5 v1.0 is a faithful seal of the original MQL5 gate"* — is **correct about the mechanical content** (H1/M1, adaptive tolerance, average-pivot level, mandatory MSS all genuinely match T3's code) but never checked *which hypothesis number* T3 actually belongs to. `kb.json` was consulted in that same evidence chain only once, for a single string ("XAUUSD M5") — its H-07/H-08 records were never read (confirmed: grepped the full DEVQ-01/annex/ADR/CS-review/pre-ratification chain for "Gate 7", "Gate 8", "kb.json" content beyond that one citation — nothing).

## What I could NOT verify (explicitly flagged, not guessed)
The original `LiquiditySweepGate.mqh` (Gate 7) predates this git repo's history entirely (the repo's first commit is a pre-existing "v3.16.4 baseline" already missing that file) — **I cannot read its actual original content.** My belief that Gate 7 was simpler (no MSS) and closer to the evidenced v1.1 definition rests on: (a) T3's header calling it merely an "EQH/EQL pool engine" absorbed as "an alternative sweep-anchor **source**" (language describing a data input, not a full state machine); (b) H-07's own kb.json hypothesis text naming no MSS/FVG/tap stage; (c) the evidenced 324-trade Python population (independently, unambiguously H-07 per its own matching evidence artifacts) genuinely has no MSS stage. This is a well-supported inference, not a certainty — I am not asserting it as fact, only as the most likely reading given (a)–(c).

## Why this matters
If confirmed: **§5 v1.0 is H-08's frozen mechanical definition, mislabeled throughout NP-ADR-008 and the DEVQ-01 chain as "H-07 v1.0."** Consequences, if so:
- NP-ADR-008 §2's provenance chain ("§5 v1.0 faithfully documents the historical MQL5 gate T3_SweepFVGGate.mqh... the divergence exists entirely within the Python lineage; v1.0 is not at fault") is not wrong about the *mechanics* but is wrong about *whose* v1.0 this is.
- The whole "v1.0 vs v1.1, same hypothesis two versions" framing (§2.1's three non-equivalence statements, adopted into my two H-007 registrations' `outcome_interpretations` this session) remains **internally true as worded** (it never asserts which hypothesis number v1.0 belongs to) but sits inside a provenance narrative that needs the correction above.
- **H-08 is not just one of "the 17 counted-only entries"** — it already has a real, sealed, IVF-drilled-source mechanical definition (T3, "§5 v1.0" once correctly re-attributed) sitting unused. Whether that changes how H-08 should be registered this sprint (bare counted-only entry vs. something richer) is exactly the kind of question this DEVQ raises rather than decides.
- My own two already-registered H-007 hypotheses (`01KYSETR2C85MRRVWZCM8V0GMC` prediction, `01KYSETR3VACRBCWN9QYRRX6DW` E2 existence) do **not** appear to need re-registration — their content describes the v1.1 evidenced definition and the genuinely-H-07 324-trade population correctly; only the *ADR's own narrative* about what v1.0 represents needs correcting. I have not touched either registration and am not proposing to.

## What this blocks (per the Owner's direction, 2026-07-30, this chat)
Per instruction, **not registering anything further** (deliverable 6, the 17 counted-only entries — including H-08 specifically) until this identity question resolves. DEVQ-NP-003 is updated separately to record that the *roster* sub-question is resolved (kb.json, Option A, per the Owner) but registration stays held behind this DEVQ.

## What I have NOT done
Not edited NP-ADR-008, `ops/DEVQ-01_NP-S1.md`, or any other normative document (outside my write authority). Not registered any of the 17. Not re-touched the two already-registered H-007 hypotheses. Not attempted to reconstruct the deleted `LiquiditySweepGate.mqh`'s content.

## Recommendation (no authority to rule — Architect/Owner call)
An appended correction (P5 — new record, `T3_SweepFVGGate.mqh` and its §5 v1.0 text both stay frozen and unedited) to NP-ADR-008 §2, re-attributing §5 v1.0 to H-08 and recording that H-07's original gate (`LiquiditySweepGate.mqh`) was deleted pre-repo-history with only its pool-formation logic surviving (merged into T3 as an anchor-source option, still gated behind T3's mandatory MSS). This does not change anything already ratified about v1.1 or the two H-007 registrations — it corrects who v1.0 belongs to.
