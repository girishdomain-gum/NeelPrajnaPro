# H-007 — neelprajna liquidity sweep, E2 EXISTENCE claim (NP-ADR-008 SS5 v1.1).
# PRE-REGISTERED and COUNTED this sprint; JUDGED only when N2 null machinery
# certifies (NP-ADR-008 SS6). One of TWO sealed registrations for this lineage
# (Execution Plan SS4 deliverable 3) — the companion prediction claim is a
# separate record (h007_np_liquidity_sweep_v1_1_prediction.yaml). Editing any
# field below re-registers as a NEW hypothesis id.
#
# NULL-MACHINERY GAP (honestly recorded, not hidden): this Kernel's hypothesis
# schema and EvidenceBattery/PlaceboBattery have no representation of ECF claim
# forms (E1/E2/E3) or the N2 block-resampling null design at all — the ONLY
# placebo methods this Kernel currently runs are DIRECTION_PERMUTATION and
# ENTRY_TIME_SHUFFLE (qrf/kernel/battery/placebo.py), neither of which is N2.
# `placebo_method` is therefore deliberately OMITTED here (not set to an
# unsupported value) — this hypothesis is registered to spend its trial
# honestly (QRF-ADR-011: registration spends the attempt at conception,
# whatever the verdict later says) and is NOT run through the Battery this
# sprint. The execution/split_spec/thresholds fields below exist only to
# satisfy the current schema's generic (prediction-shaped) required fields;
# they are NOT this claim's actual test — the actual test (arrangement, not
# profitability) awaits N2 machinery that does not exist yet in this Kernel.

lineage: h007_np_liquidity_sweep_v1_1

scope: xauusd_m5_vantage

window: "01KYSEDSM6K5ZKWK0XRCC4SVZ7"  # TRAINING [2026-04-20T22:00:00Z, 2026-07-10T14:33:00Z) on xauusd_m5_vantage

instruments:
  - neelprajna.liquidity_sweep@1.1.0

setup_dsl:
  event: neelprajna.liquidity_sweep.sweep
  claim_form: E2_existence
  testable_content: >-
    does SWEEP follow POOL_FORMED beyond chance arrangement -- do
    penetration-and-reclose events concentrate at previously frozen
    equal-high/low levels more than a null preserving session and volatility
    character while destroying the cross-bar level relationship (NP-ADR-008
    SS6)
  null_design: N2_block_resampling_seam_preserving_calendar_template   # NOT YET IMPLEMENTED in this Kernel -- see header

# Placeholder mechanical fields only (this claim is not run this sprint by
# these fields -- see header). Mirrors the prediction claim's own honest
# no-stop/no-target choice (NOTE-NP-001) for consistency, not because these
# values encode the E2 test itself.
execution:
  hold_bars: 12
  size: 1.0
  strength_min: 0.0
  stop_offset: null
  target_offset: null
  exit_rule: time_stop

cost_model_ref: xauusd_retail_h07

split_spec:
  n_folds: 4
  embargo_bars: 15

thresholds:
  min_n: 100
  base_alpha: 0.05
  correction:
    method: bonferroni

# No placebo_method: N2 is not a DEVQ-018-ruled method this Kernel's
# PlaceboBattery can run (see header). Deliberately omitted, not guessed.

# v2 pre-commitments -- written BEFORE running, and before N2 machinery exists.
thesis: >-
  SWEEP events (NP-ADR-008 SS5 v1.1: wick penetrates a frozen equal-high/low
  pool level by >=5 ticks and recloses within the reclose window) concentrate
  at previously frozen pool levels beyond what a null preserving session and
  volatility character -- while destroying the cross-bar level relationship --
  would produce (block-resampling null N2, seam-preserving, calendar-template;
  block length, seeds and thresholds to be sealed in a future YAML revision
  when N2 machinery exists in this Kernel). THREE NON-EQUIVALENCE STATEMENTS
  (NP-ADR-008 SS2.1, Chief Scientist, adopted verbatim -- must survive into
  every derived artifact): (1) this v1.1 result is NOT equivalent to the
  original SS5 v1.0 hypothesis; (2) it is a NEW hypothesis bound to the
  documented v1.1 detector lineage; (3) any future judgment of the original
  T3/MSS detector requires a separate implementation and fresh out-of-sample
  evidence. No verdict under v1.1 validates, corroborates, or speaks to the
  historical T3 gate.
family: xauusd/neelprajna
observatory_ancestry: []
outcome_interpretations:
  PASS: >-
    NOT YET JUDGEABLE this sprint -- N2 null machinery does not exist in this
    Kernel. If/when N2 certifies and this claim is re-run under it: a PASS
    would mean sweep-at-pool-level concentration exceeds chance arrangement --
    existence of the POOL_FORMED -> SWEEP relationship beyond the definition
    trap (pool_tol/min_pen buy the base rate, never the arrangement).
    Establishment licenses mechanism and predictive investigation; it licenses
    no trading conclusion. THREE NON-EQUIVALENCE STATEMENTS (verbatim,
    NP-ADR-008 SS2.1): v1.1 is not equivalent to the original v1.0 hypothesis;
    it is a new hypothesis bound to the documented v1.1 detector lineage; any
    future judgment of the original T3/MSS detector requires a separate
    implementation and fresh out-of-sample evidence.
  FAIL: >-
    NOT YET JUDGEABLE this sprint -- N2 null machinery does not exist in this
    Kernel. If/when N2 certifies: a FAIL would mean the arrangement is
    indistinguishable from the seam-preserving null -- the definition trap
    caught the base rate, not real structure. THREE NON-EQUIVALENCE STATEMENTS
    (verbatim, NP-ADR-008 SS2.1): v1.1 is not equivalent to the original v1.0
    hypothesis; it is a new hypothesis bound to the documented v1.1 detector
    lineage; any future judgment of the original T3/MSS detector requires a
    separate implementation and fresh out-of-sample evidence.
  INSUFFICIENT: >-
    Correct and current status as of this registration: N2 null machinery does
    not exist in this Kernel, so this claim cannot be judged at all this
    sprint -- registered and counted only (QRF-ADR-011), per NP-ADR-008 SS6.
    THREE NON-EQUIVALENCE STATEMENTS (verbatim, NP-ADR-008 SS2.1): v1.1 is not
    equivalent to the original v1.0 hypothesis; it is a new hypothesis bound to
    the documented v1.1 detector lineage; any future judgment of the original
    T3/MSS detector requires a separate implementation and fresh
    out-of-sample evidence.
