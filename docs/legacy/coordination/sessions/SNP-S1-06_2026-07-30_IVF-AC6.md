# Session SNP-S1-06 · Sprint NP-S1 · 2026-07-30
Role: **IVF / Validator** (not Developer). Worktree `ivf-validator-neelprajnapro-a9bdfe`,
fresh session, no prior context. Instruction: `ops/ARCH-NP-002_IVF_instruction_AC6.md`.

## Outcome (one line)
Executed ARCH-NP-002 (AC-6) in full: six-plant+control drill CAUGHT clean, then
independent re-derivation of verdict `01KYSGQR3D8SYSVJFSF9M77CMY` reproduced every
ARCH-quoted figure to 1e-9 (chain checks all GREEN), but two of §3's four "unchecked"
items did not survive: the three non-equivalence statements are not byte-verbatim in
either H-07 registration, and an independent SWEEP recount disagrees with the reported
325 by 6 events (331). **Overall IVF verdict: RED.** Report:
`ivf/reports/IVF_NP-S1_AC6.md`.

## What happened
1. `git fetch origin` + merge — already up to date at `9b2df73` (T-041, the ARCH-NP-002
   issuance commit).
2. Read the sealed instruction and every normative text it cites: NP-ADR-008 (`ops/NP-ADR-H07_definition_v1.1_draft_v2.0.md`, ratified), its Appendix A
   (`ops/NP-ADR-008_APPENDIX-A_provenance_correction.md`), Execution Plan §0,
   V&V Plan §§1–3, and the Roles doc.
3. **Provenance finding, surfaced immediately:** the target verdict and its burn are
   NOT present on `main` (or this branch) at `9b2df73` — the Battery run that produced
   them lives only on the unmerged branch `claude/np-adr-008-liquidity-sweep-7aa72b`
   (commit `2e8d40a`), whose worktree is also the only place on this machine holding the
   gitignored `datastore/bulk/verdict_trades.h007_np_liquidity_sweep_v1_1/` and
   `datastore/bulk/xauusd_m5_vantage/` parquet outputs. Read those files read-only from
   that worktree; wrote nothing there. Recorded in the report §0 so a future auditor
   working from `main` alone isn't misled into thinking the data is missing/fabricated.
4. Built `ivf/checks/check_np_s1_ac6.py` — independent re-derivation, zero `qrf` imports.
   Reimplemented (never imported): `deflation.py`'s stated family prefix-segment rule,
   `seeds.py`'s stated `for_run` recipe, `record.py`'s stated `canonical_bytes`/
   `content_hash` chain (both modules' own docstrings say, verbatim, that the IVF
   re-implements them independently). Read (not imported) `battery.py` only for the
   statistics engine's mechanical bootstrap-CI recipe (`default_rng(seed)`, 2000
   resamples, `percentile([2.5,97.5])`) since ARCH-006's normative text underspecifies
   it below 1e-9 tolerance — this is Kernel judge machinery, not detector/domain logic,
   so it does not trip the "never learn what a sweep is from the Python" rule.
5. Built `ivf/checks/drill_np_s1_ac6.py` (pattern from the existing `ivf/checks/drill_s6.py`):
   six plants (altered trade net; family_m 19→18; deleted trial_count; cost model
   swapped to $0.26 baked into the parquet; window `ts_end` shifted one bar; hypothesis
   thresholds edited post-hoc) + one untampered control, each a scratch copy under the
   already-gitignored `ivf/reports/drill_ac6_tmp/`. **All six caught, control clean** —
   ran BEFORE the real re-derivation, per instruction.
6. Ran the real re-derivation: every ARCH-quoted figure (n=259, net mean, gross mean,
   p, t, CI, family_m/effective_alpha, fold n/means, verdict FAIL) matched to 1e-9 or
   better. Hash-chain intact (112 records, genesis to tail). Burn atomic. Cost model,
   window bounds, embargo, seed-reproducibility all PASS.
7. Built `ivf/checks/sweep_recount_np_s1_ac6.py` for §3 item 3 — SWEEP count from the
   16,029 M5 bars using NP-ADR-008 §3's text alone (never opened `np_feature_service.py`
   / `np_probability_engine.py`). Result: 331 vs the reported 325 — a disclosed,
   ADR-text-consistent disagreement (the ADR's own `swings` input and pool-clustering
   mechanics are underspecified; three assumptions are named in the script's docstring
   and the report).
8. Spot-checked bar-build honesty (§3 item 4) against the raw Stage2 tick parquets at
   `F:\NeelPrajna\Validation\Stage2\parquet\` (60 files, external to this repo but
   present on this machine — only raw tick columns read, no Developer Python). First
   bar, last bar, and one weekend seam (Fri 2026-06-19T17:00Z → Sun 2026-06-21T22:05Z)
   all reproduce exactly / show a genuine closure gap.
9. Checked §3 item 1 (undeflated alpha): p=0.0574 > base_alpha=0.05 — FAIL survives
   without deflation; the 19-vs-18 ruling could not have changed the outcome.
10. Checked §3 item 2 (three non-equivalence statements verbatim): **FAIL** — both H-07
    registrations carry the statements' substance but as one semicolon-joined clause,
    not NP-ADR-008 §2.1's three sentences byte-for-byte (statement 1 drops the "E2-"
    prefix; statements 2–3 are lower-cased at the clause boundary). 0/3 exact substring
    matches in either registration.
11. Wrote `ivf/reports/IVF_NP-S1_AC6.md` (drill results, all 8 chain checks + summary
    table, all four §3 items, what could not be verified, non-goals confirmed, overall
    verdict RED — named plainly, not softened, not averaged, does not proceed to HC).
12. This log; committing and pushing now.

## State for the next session
- **IVF verdict on AC-6 is RED.** Two items need Architect/Owner disposition before this
  lineage is relied on further: (a) the registration wording gap against NP-ADR-008
  §2.1's "verbatim" requirement — likely a quick fix (reproduce the three sentences
  exactly) but is a normative-document-adjacent registration, so it is the Architect's
  call, not this session's, to touch; (b) the 331-vs-325 SWEEP recount disagreement,
  which cannot be resolved by IVF without either reading the Developer's Python (ruled
  out by design) or the Architect/Developer clarifying the `swings` construction and
  pool-clustering mechanics in NP-ADR-008 §3's text itself.
- **Separately and importantly:** `claude/np-adr-008-liquidity-sweep-7aa72b` (commit
  `2e8d40a`) carries the actual Battery run this instruction is about and is NOT yet
  merged to `main`. Whoever merges it should be aware the verdict this IVF just checked
  currently exists on that branch only.
- No code written under `qrf/**`. No new hypotheses/registrations/runs/burns. No
  normative-document edits. Confirmed exactly one `window_burn` exists for this
  (window, lineage) — no second Battery run occurred, here or elsewhere, during this
  session.

## Definition of Done status
This session's scope (ARCH-NP-002 / AC-6 IVF check) is complete: drill run and clean,
real re-derivation run and reported, report written with an honest RED verdict, this
log written. Not done (out of this session's scope): disposition of the two RED
findings, and the `main` merge of the branch holding the actual Battery output.
