# S9-2 · session log · 2026-07-26 · developer (claude-code)
Instruction: ARCH-009 (Sprint 9 — Second Lens, Rebuild, Enforcement).
Branch: `claude/arch-009-hypothesis-schema-v3-cebf6a` · Pushed through: commit `a635364`
(schema v3 + calendar exit + multi-window battery + overlap engine + H-004 + rebuild).

Boot per CLAUDE.md: `git fetch` OK; `origin/main` already up to date; then MERGED the
prior S9-1 work (`origin/claude/arch-009-t0-execution-59fec3`, through `74f6217`) into
this branch to continue from where S9-1 handed off (T0 + §1 + §2 + §4.2 + §4.1
foundation + DEVQ-022 done). Read PROTOCOL v1.3, state file (stale, pre-S3 — noted),
S9-1 log, ARCH-009 FINAL + both addenda + CLOCK DOCTRINE, DEVQ-020/022, the sealed
pre-registration notes. Env note: the worktree `.venv` is missing scipy; used the main
venv `/f/QRF/.venv/Scripts/python.exe` with `PYTHONPATH=.` throughout (has all deps).
Rebuilt the gitignored parquets (lens feeds + xauusd_h1_full) from the raw CSVs first.

## DONE (committed + pushed)

**§4.3 machinery — multi-window hypothesis schema v3 + calendar exit (commit 822c5f1).**
DEVQ-022 Option A, real kernel surface with its own tests + firewall pass:
- `schemas.py`: hypothesis v3 (v2 shape + `window_refs` list) and verdict v3
  (`window_refs` + `n_dropped_hole`, `window_ref` retained = primary); `execution.exit_rule`
  optional field. All additive — (hypothesis,1/2), (verdict,1/2) untouched; H-001/2/3
  unchanged.
- `hypotheses.py`: the registry resolves `window_refs` (disjoint + ordered, seam order),
  seals the union BOTH in the payload and in the record parents (asserts `parents ==
  tuple(window_refs)`), requires the v2 pre-commitments, and is idempotent + tamper-checked
  on the list. Single-window (`window`) path unchanged.
- `splits.py`: `walk_forward_multi(window_lengths, spec)` — per-window anchored folds over
  the concatenated index space, embargo WITHIN each window, the seam a HARD fold boundary
  (no test block straddles it, no train reaches into another window). A single window
  reduces exactly to `walk_forward`. Property-tested over a shape matrix.
- `battery.py`: `_hypothesis_windows`, `_check_designations` (every window
  TRAINING/EXPLORATION), the multi-window `_pipeline` (per-window folds pooled into one
  evidence set + tri-state), `_count_hole_drops` (structural `n_dropped_hole` = trades whose
  same-day exit would cross an inter-window seam), ONE `window_burn` PER window, and the
  verdict v3 payload. Single-window verdict path (v1/v2) byte-unchanged.
- `engine.py`/`fills.py`: `exit_rule="calendar_day"` — exit at the OPEN of the LAST bar
  sharing the entry bar's epoch-day (the SAME index the SeasonalityDetector uses for its dow
  markers), capped at `hold_bars` (so embargo ≥ hold+1 still clears it), DROPPED if the day
  is truncated by the data tail (the hole / a fold boundary — never a fabricated early exit).
  Default `time_stop` untouched; the selftest gate stays green; the placebo's
  entry_time_shuffle recomputes the exit from the shuffled entry, so the null stays valid.
- Tests (37): 10 registry multi-window, 9 splits multi, 6 H-004 config sanity, 5 calendar
  exit, 4 multi-window battery (union PASS, N-burns, verdict v3, re-run refusal on either
  window, VIRGIN-member refusal, hole-drop count). Firewall GREEN; ruff clean.

**§4.3 — H-004 registered + judged, placebo-first (commit ed151da).**
`configs/hypotheses/h004_dow_monday_drift_v2.yaml` (v3 multi-window over the 2024+2025
training union; calendar exit; `placebo_method: entry_time_shuffle` sealed per §2; min_n=45
per DEVQ-022 Q2; OBS-1 interpretation guard baked into outcome_interpretations) +
`scripts/judge_h004_s9.py`. Bars fed from `xauusd_h1_primary_full`; its 2024-training slice
ASSERTED byte-identical to the 2024 window's own manifest (`xauusd_h1_full`) — the
reserve-by-market-time doctrine. 72 Monday markers in the union (== DEVQ-022's calendar
count). Judged on the real journal (fresh lineage → both training windows available):
- hypothesis `01KYDH7SGVXNTKDCZE2K84XGCD` (schema v3)
- placebo `01KYDH7T3H2BT82FDNSJX80WSB` (entry_time_shuffle, **1/20** PASS — a healthy null)
- verdict `01KYDH7T6SH1D0AJMA70M2H0P8` — **FAIL** (n=56 == the ~56 DEVQ-022 predicted;
  net mean **+5.00**/trade but one-sided **p=0.108 > deflated α=0.05**: no edge over RANDOM
  TIMING, the OBS-1 arbiter). `n_dropped_hole=0` (no Monday spans the seam), `n_dropped_tail=2`.
- burns `[2024-training, 2025-training]` (one per window); belief `01KYDH7T8MQ7GYKHBXA6EGQMKZ`
  REJECTED 0.784. Journal **70 records, chain GREEN**; both VIRGIN reserves UNTOUCHED
  (no VIRGIN window burned). All three tri-states were acceptance-valid; FAIL is the data.

**§4.1 — clock-era overlap engine BUILT + RUN; second_lens append BLOCKED (commits
e7fd0bc, 33bd05e).** `scripts/overlap_second_lens_s9.py` implements the sealed
pre-registration faithfully (reserve exclusion on the server clock, DST-era segmentation,
per-era integer-hour shift by max shared-count with the 5% runner-up guard, OHLC agreement
0.50/0.75). It computed the alignment on the TRAINING timespan and **the 5% ambiguity guard
fired in ALL FOUR eras** — a dense hourly grid makes adjacent integer-hour shifts nearly
equally populated by shared-COUNT (era margins 2.13%–4.36%, all < 5%). Per the seal ("do not
silently pick"), the engine self-STOPS and appends nothing. Raised **DEVQ-023** (BLOCKER,
§4.1 ONLY). The sealed OHLC-agreement metric resolves the shift UNAMBIGUOUSLY and confirms
the pre-registration's own hypothesis (−2h winter / −3h summer; agreement 0.73–0.95 for the
winner vs 0.00–0.25 for the runner-up); DEVQ-023 recommends the Architect ratify
OHLC-agreement as the alignment discriminator. The append path is deliberately inert until
ruled.

## BLOCKED-ON
**DEVQ-023** (inbox/OPEN) — §4.1 second_lens alignment discriminator. §4.3 and everything
else are NOT blocked by it (H-004 never touches the second feed).

## NOT DONE this session (handoff)
- §4.1 `second_lens` append — awaits the DEVQ-023 ruling, then wire the ratified
  discriminator, run the overlap (TRAINING timespan only, reserves excluded), append the
  first `second_lens` ({source tier=BROKER, overlap_manifest, per-era shift tables in
  agreement_summary.notes}).
- §1 (rebuild-bulk) + §2 (placebo_method seal) were completed by S9-1 (merged in, GREEN).
- Sprint NOT closed / NOT merged to main: the sprint AC requires the `second_lens` record,
  which is DEVQ-blocked. Held open for the ruling — mirrors S9-1's Owner-gate handoff.

## Tests / hygiene
Full suite: GREEN (835 passed, 0 failed, 0 errors, 0 skipped), including 37 new S9-2 cases + the §1
rebuild suite extended to h004 (all 4 lineages sha assert-equal, cross-process stable).
Firewall 8/8 GREEN. Ruff clean on every file I changed;
one PRE-EXISTING E501 in `scripts/t0_s9.py:59` (a long string literal from the S9-1 merge,
not touched this session — flagged for its owner). Journal 70 records, chain GREEN; VIRGIN(s)
untouched. No writes outside permitted paths (qrf/ production + tests, scripts/, configs/
hypotheses YAML, journal via RecordStore.append only, inbox/OPEN DEVQ-023, this log).

## State at session end
Commits `822c5f1` (schema v3 + calendar exit) · `e7fd0bc`/`33bd05e` (overlap engine + DEVQ-023)
· `ed151da` (H-004) + this log, all pushed to
`origin/claude/arch-009-hypothesis-schema-v3-cebf6a`. Sprint 9 remains OPEN on DEVQ-023 for
the §4.1 second_lens; §4.3 (multi-window schema v3 + H-004) is COMPLETE.
