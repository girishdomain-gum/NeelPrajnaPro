# S5-2 · session log · 2026-07-25 · developer (claude-code)
Instruction: Owner command — two DEVQ-012 fill-rule refinements (confirm or implement)
Branch: claude/arch-005-t0-execution-7549cc · Pushed through: (this session, then merge)

CONTEXT: At boot DEVQ-012 was still OPEN on my branch. Mid-session, on the merge to
main, the Architect's ruling arrived on origin/main (commit f3ff5c9 "DEVQ-011/012/013
ruled and closed"): DEVQ-012 **A RATIFIED** with exactly these TWO clarifications to
confirm — (1) pessimistic gap-through both ways, (2) n_dropped_tail reporting. So the
Owner's command was relaying the Architect's ruling, and my implementation matches
both clarifications precisely. Merged origin/main in; resolved the DEVQ-012 conflict
(Architect's CLOSED reply is authoritative) and added a developer CONFIRMATION under
it. DEVQ-011 (A ratified; binding S6 rule embargo_bars>=max hold_bars+1, battery-side
— NOT a splits change) and DEVQ-013 (A ratified) need no code this sprint.

DONE (both refinements implemented + tested):
- **Pessimistic gap-through, both ways** (qrf/trading/simulator/fills.py). A level
  the market GAPS THROUGH fills at the trader-adverse of {level, bar open}, both
  exit types + both directions: STOP → worse of stop/open (long min, short max, so
  an adverse gap fills beyond the stop); TARGET → capped at the level (a favorable
  gap-open is never credited). Gaps can only hurt, never help. Non-gapping touch
  still fills at the exact level. +6 tests (long/short stop gap; long/short target
  favorable-gap cap; non-gap touch unchanged).
- **n_dropped_tail reporting** (qrf/trading/simulator/engine.py). Eligible events
  the data tail cannot open+close (no next bar, or time-stop exit beyond the data)
  are never filled on absent bars AND are now counted in Trades.n_dropped_tail,
  carried in the canonical image — visible, not silent (screener trial_count
  discipline). +2 tests (no-exit + no-entry tail count; presence in canonical
  payload); the existing dropped-trade test now asserts n_dropped_tail == 1.

IN-PROGRESS: — (session closing after merge + push)
BLOCKED-ON: -   NEXT: DEVQ-011/012/013 all CLOSED (ratified) — nothing outstanding
from them this sprint; DEVQ-012's two clarifications confirmed implemented+tested.
Then IVF S5 / Drill S5 / HC; Owner Go/No-Go → GO-S5. For S6: encode DEVQ-011's
binding embargo_bars>=max hold_bars+1 as a battery-side validation (recorded in the
ruling; not a splits.py change).
Tests: 655 passed (+7 this session), 1 warning (pre-existing pandas_ta deprecation)
· ruff clean · firewall GREEN · journal 26 records, chain GREEN (no journal writes
this session — code + tests + one DEVQ addendum + this log only).

No writes outside permitted paths: qrf/trading/simulator/{fills,engine}.py +
tests/simulator/test_engine.py (code/tests), DEVQ-012 addendum (my own OPEN thread),
this session log. No journal appends; no docs/ edits outside inbox/sessions.
