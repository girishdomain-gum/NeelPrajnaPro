# S8-1 · session log · 2026-07-25 · developer (claude-code)
Instruction: ARCH-008 (Sprint 8 — Graduation + Placebo + Family Wave 1) ·
Branch: claude/qrf-architect-handover-cf5806 ·
Pushed through: **NOT PUSHED — origin unreachable this session** (see BLOCKED-ON).
Local commits d24ab5a..(HEAD) below must be pushed when network returns.

CONTEXT: Booted per CLAUDE.md. `git fetch origin` FAILED (no network); proceeded
on local state, which matched the handover's Section-0 snapshot exactly (local
main == HEAD == d24ab5a, journal 41, inbox empty, chain GREEN). Owner boot
one-liner interpreted as "execute as the Developer" (CLAUDE.md is the Developer
charter). Worktree had no .venv and no gitignored bulk parquet; used the main
venv (F:/QRF/.venv) and rebuilt datastore/bulk/xauusd_h1_full from the CSV
(hash-verified against the existing manifest, 5938 rows). Scripts need
`PYTHONPATH=<worktree>` so the editable `qrf` resolves to worktree code, not main.

DONE (ARCH-008 §T0, §1, §2, §3; §4 partial):
- **T0** — appended the GO-S7 note (01KYCNVX45TCMS16D22T2NEP3H), parent the GO-S6
  note; journal 41→42, chain GREEN. (commit "S8 T0")
- **DEVQ-018..021** — the four gated choices, each with a firm recommendation
  (proceed-on-recommendation, QUESTION level); DEVQ-021 raises a scoped BLOCKER on
  §4 only (offline). All OPEN, awaiting Architect ratification at REV-S8.
- **§1 Placebo battery (G-3)** — `qrf/kernel/battery/placebo.py`. Refactored the
  battery: steps 4-7 extracted into a shared `_pipeline`, and a new non-writing
  `evaluate()` (no verdict, no burn) so the placebo is PROVABLY the same judge as a
  verdict. Two seeded nulls (DEVQ-018): `direction_permutation` (directional event
  claims), `entry_time_shuffle` (fixed-direction timing claims). `placebo_run`
  record; type-audited to consume no window. 7 tests. AC met: ~0 null passes under
  deflation, ~alpha (1/20) without.
- **§2 Graduation gate (G-1)** — `qrf/kernel/graduation/`. `Promoter.promote`
  appends a `promotion` ONLY when all four gates hold: (a) PASS verdict of the
  claim; (b) placebo of the same hypothesis with no excess null passes (binomial
  ceiling at the verdict's deflated alpha — G-3 feeds G-1); (c) `second_lens` whose
  `overlap_manifest` resolves to a real `bulk_manifest`; (d) the (family, claim)
  belief cites the verdict and is not CONTESTED. Beliefs stay verdict-only. Each
  missing leg tested for refusal (7 tests). No real second feed exists → promotion
  is impossible today, by design.
- **§3 Family Wave 1** — registered + judged on the REAL TRAINING window (fresh
  lineages), each with its placebo:
  - **H-002** `h002_fvg_intraweek_follow_through` (01KYCQBGW3C1M9ARZ1Y320WXW4),
    family xauusd_h1/smc.fvg, N_trials 502 → eff.alpha ≈ 9.96e-5. Setup = H-001
    minus weekend-born FVGs (the scan's OWN `_spans_weekend`, reused not
    paraphrased). **VERDICT FAIL** (verdict 01KYCQBHRJHY1A1PY1PQ01TAT5; n=637,
    net −0.5247/tr, total −334.26, p=0.9312). Belief REJECTED strength 0.8624
    (01KYCQBHSWWW50DKGCHATQ4C67). Placebo direction_permutation **0/20**
    (01KYCQBHMFK24B65JW4Y3BQJMR). Window burn 01KYCQBHS6T92G5PHDVE6X01DS.
  - **H-003** `h003_dow_monday_drift` (01KYCQBHTPAFQX3DZHJE2BEK28), FRESH family
    xauusd_h1/seasonality.calendar, N_trials 0 → eff.alpha 0.05 (the DEVQ-017
    zero-deflation boundary in the wild). Setup = `seasonality.dow.mon` lifted to
    LONG at next-open, hold 22, embargo 23 (DEVQ-019). **VERDICT INSUFFICIENT**
    (verdict 01KYCQBJ7N2D99N7CDKQ1V4J1K; n=28 < min_n 40; net +2.5725/tr, p=0.2602).
    Belief UNTESTED strength 0 (01KYCQBJ8Z7CAB2K26SRKK0KVH). Placebo
    entry_time_shuffle **6/20** (01KYCQBJ3Z272XDF96BXGH0N41). Window burn
    01KYCQBJ8APY08BQBZF8P2VDNQ.
  - Journal 42→54, chain GREEN. VIRGIN (01KYB4SSD9VVKB577KRGB1W1P0) UNTOUCHED; all
    three burns (h001/h002/h003) are on the TRAINING window.
- **§4 Cross-impl FVG check** — `tests/concepts/test_smc_cross_impl_s8.py`. Ships
  the WRITTEN definitional-difference map, a tested reconciliation harness, and an
  independent clean-room ICT 3-candle FVG re-implementation that reconciles to FULL
  agreement with our smartmoneyconcepts-wrapped detector. The REAL external-library
  leg (smc-toolkit) is gated by `pytest.importorskip` and SKIPS offline. **Not
  fully closed** — see BLOCKED-ON / DEVQ-021.

IN-PROGRESS: sprint close (this log, completion report, gen_state, merge). Push
pending network.

BLOCKED-ON:
- **DEVQ-021 (§4)** — the independent library (smc-toolkit) is NOT installable
  offline and must be distinct from smartmoneyconcepts (which our detector wraps).
  §4's external-library reconciliation is deferred to a session with network; the
  harness + difference map + clean-room check are done and green now.
- **PUSH** — `git fetch/push origin` fail (no network). All commits are local only;
  the "visible on origin" guarantee (NOTE-005) is temporarily unmet by force
  majeure. Flush all S8 commits on reconnection.

NEXT (Architect, per ARCH-008 §Sprint close): IVF S8 (independent placebo
recomputation from the recorded seeds; promotion-gate audit; H-002 weekend-filter
recomputation against the scan rule) + Drill S8 (planted placebo-pass-hidden +
planted promotion-missing-a-leg; DRILL FIRST; clean control) + HC caption fix +
visual HC of the wave's trades. Ratify DEVQ-018..021. Note for the IVF: H-003's
entry_time_shuffle placebo shuffles the full 52-Monday event count into the
TRAINING window (the real judge sees ~28 in-window), so it is a CONSERVATIVE
(more-powerful) null — 6/20 flags that 2024's gold drift lets random-timing
long-hold PASS ~30%, which is exactly why min_n INSUFFICIENT (not a looser
threshold) is the honest call.

Tests: 774 passed (760 S7 + 7 placebo + 7 graduation; §4 adds 3 pass + 1 skip) ·
ruff clean · firewall GREEN · journal 54 records (chain GREEN) · VIRGIN untouched.

No writes outside permitted paths: qrf/kernel/{battery/battery,battery/placebo,
records/schemas,errors}.py + qrf/kernel/graduation/** (code); configs/hypotheses/
h002,h003.yaml; scripts/{t0_s8,judge_family_wave1_s8}.py; tests/{battery/test_placebo,
graduation/**,concepts/test_smc_cross_impl_s8}.py; docs/coordination/inbox/OPEN/
DEVQ-018..021 + sessions/ (this log); the ARCH-008 completion-report append;
docs/handover/AI_PROJECT_STATE.md via gen_state only. journal.jsonl appended only
through RecordStore.append.
