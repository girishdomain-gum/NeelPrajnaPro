# Session SNP-S1-04 · Sprint NP-S1 · 2026-07-30
Developer: claude-code, worktree `claude/np-adr-008-liquidity-sweep-7aa72b` (continuation of SNP-S1-03, same conversation)

## Outcome (one line)
Owner pointed to `F:\NeelPrajna\knowledge_base\kb.json` as the roster for "the 17" (resolving DEVQ-NP-003's original question, Option A) — but flagged, and I independently verified with primary sources, a much more serious finding surfacing from that same file: **`T3_SweepFVGGate.mqh`'s own header says it "was Gate 8," while NP-ADR-008 §2 and the entire DEVQ-01 evidence chain attribute it to H-07 (Gate 7).** §5 v1.0 is very likely H-08's frozen mechanical definition, not H-07's. Raised `DEVQ-NP-004` (BLOCKER) with full evidence; **registered nothing this session**, per the Owner's explicit instruction to resolve the identity question first.

## What happened
1. Read `F:\NeelPrajna\knowledge_base\kb.json` in full (18 records, H-01..H-18) — confirmed H-07's evidence trail (`stage3_feature_service`, `probability_engine_v0` FAIL citing `Stage4_EvidenceReport_H07.json`) is unambiguously the same 324-trade population this sprint has worked with throughout, so the sprint's registered lineage (`h007_np_liquidity_sweep_v1_1`) is not in question. But H-07's own `executable_definition` field reads `"LiquiditySweepGate.mqh -> np_feature_service.py"` — not `T3_SweepFVGGate.mqh`, the file NP-ADR-008 §2 and `ops/DEVQ-01_NP-S1.md` Addendum A cite as "the historical MQL5 gate" for H-07.
2. Searched the entire `F:\NeelPrajna\repo` git history (`git log --all --diff-filter=A`) for `LiquiditySweepGate.mqh` and `SweepFVG1HGate.mqh` (H-08's cited gate) — neither has ever existed as a file in this repo. The only real file in this naming family is `T3_SweepFVGGate.mqh`, present since the repo's very first commit.
3. Read `T3_SweepFVGGate.mqh`'s header in full. It states, in its own words: *"TRIGGER T3 — 1H Liquidity Sweep + FVG **(was Gate 8)** (NY Open Playbook)"* and describes a v2.0 change that **merged in and then deleted** the original Gate 7 (`LiquiditySweepGate.mqh`)'s pool engine as one optional "anchor source," with the MSS→FVG→TAP pipeline mandatory regardless of anchor source. T3's own 5-stage doctrine matches H-08's kb.json hypothesis text ("1H sweep -> MSS -> FVG tap sequence (NY open)") far more precisely than H-07's ("equal-high/low sweep + reclose reverses" — no MSS, no FVG, no NY-open in H-07's own text).
4. Cross-checked the full DEVQ-01/annex/ADR/CS-review/pre-ratification chain for any prior mention of "Gate 7", "Gate 8", or `kb.json`'s actual H-07/H-08 content — found only a single, narrow prior citation of `kb.json` ("XAUUSD M5" string match); no session ever read its H-07/H-08 records or cross-referenced T3's own "was Gate 8" header line. This is a genuinely new finding, not a re-litigation of settled ground.
5. Raised `docs/coordination/inbox/OPEN/DEVQ-NP-004_h07_h08_gate_provenance_misattribution.md` (BLOCKER), explicitly calibrating confidence: the "T3 was Gate 8, contradicting NP-ADR-008 §2" fact is directly quotable and certain; the further inference that the original (pre-repo-history, unrecoverable) `LiquiditySweepGate.mqh` was itself MSS-free — making it a closer match to the v1.1 evidenced definition — is offered as well-supported but explicitly flagged as unverifiable (the file predates this repo's git history entirely).
6. Updated `DEVQ-NP-003` in place: recorded the Owner's roster ruling (Option A via `kb.json`) as answering its own original question, while noting registration of the 17 stays held behind the new `DEVQ-NP-004`, not this file.

## State for the next session
- **Nothing further registered this session** — deliverable 6 (the 17), the AC-4 mapping seal, the Battery run (deliverable 4), and the comparison report (deliverable 5) all remain blocked, now specifically behind `DEVQ-NP-004`.
- The two already-registered H-007 hypotheses (prediction + E2 existence, from SNP-S1-03) were reviewed against this finding and appear **not** to need re-registration — their content describes the v1.1 evidenced definition and the genuinely-H-07 population correctly; only NP-ADR-008's own narrative about what §5 v1.0 represents needs an appended correction (Architect/Owner authority, not mine).
- Recommended disposition (no authority to rule): an appended NP-ADR-008 §2 correction re-attributing §5 v1.0 to H-08, leaving the v1.0 text itself frozen and unedited (P5).
- Three open items now on record for the Architect: `DEVQ-NP-003` (roster — Owner-answered, logged), `DEVQ-NP-004` (Gate 7/8 provenance — the active blocker), `NOTE-NP-001` (engine stop/target capability gap, from SNP-S1-03).

## Definition of Done status
No deliverable work performed or attempted this session by design — the Owner's instruction was to resolve the identity question before registering anything further, and that investigation is what this session did. This log written; committing and pushing now.
