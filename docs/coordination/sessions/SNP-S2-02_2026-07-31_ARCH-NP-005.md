# Session SNP-S2-02 · Sprint NP-S2 · 2026-07-31
Role: **Developer**, worktree `h007-lineage-rebuild-bulk-009860`, branch
`sprint/NP-S2`.
Instruction: `ops/ARCH-NP-005_fix_rebuild_bulk_h007.md`, fixing
`docs/coordination/notes/NOTE-NP-003_rebuild_bulk_missing_h007_lineage.md`.

## Outcome (one line)
ARCH-NP-005 complete: `scripts/rebuild_bulk.py` now dispatches the h007
lineage (`_LINEAGE_DATASET` + `_events_for_lineage`), backed by a new
`ingest_h07_m5_vantage.rebuild_bulk()` bars-rebuild step; `rebuild_all()` no
longer raises on invocation; both previously-failing `test_rebuild_bulk_s9.py`
tests pass, a new named regression test proves the h007 manifest reproduces
byte-identically, full suite green (887 passed, 1 pre-existing unrelated
failure), firewall green; handover published at
`ops/aro/handovers/ARCH-NP-005/HANDOVER.md`.

## What happened
1. `git fetch origin`; my branch was behind `sprint/NP-S2`'s actual tip
   (`2a59e9d`, T-057) by six commits (T-052 through T-057) — reset to match
   the fetched tip before reading anything, per the boot sequence's
   freshness-first rule. Confirmed `491955f` (the commit containing my
   instruction, per the human boot prompt) was an ancestor.
2. Found `CLAUDE.md`'s own line 6-8 (pointing at `docs/coordination/
   PROTOCOL.md` v1.3) stale — that file no longer exists at that path (only
   archived under `docs/archive/gen1/`); the live governing document is now
   `docs/roles/NeelPrajnaPro_Roles_And_Communication-v1.0.md`, per the
   restructure the rest of `CLAUDE.md` already documents (NOTE-NP-004,
   the retired `docs/handover/AI_PROJECT_STATE.md` step). Read that instead.
   Read the latest session log (`SNP-S2-01_..._WO-P.md`), `ARCH-NP-005`,
   `NOTE-NP-003`, and `tests/scripts/test_ac1_engine_parity_np004.py` (the
   named reference for the byte-identical h007 pipeline calls) before
   writing any code.
3. `uv sync` (fresh worktree, no `.venv` yet — one-time; direct
   `.venv/Scripts/python.exe` invocation from then on).
4. Ran the two named-failing tests first to see the exact failure shape:
   `test_rebuild_all_matches_recorded_manifests` failed on a stale hardcoded
   `assert len(recorded) == 4` (the h007 verdict already had a
   `trades_manifest` in the journal — `len(recorded)` was already 5,
   independent of my fix); `test_rebuild_byte_stable_across_process_restart`
   failed on the documented `SystemExit`.
5. Traced the h007 pipeline: bars come from `ingest_h07_m5_vantage.
   build_m5_bars(TICK_DIR)` (dataset `xauusd_m5_vantage`); its
   `bulk_manifest` is already in the journal from NP-S1, but — confirmed by
   direct `ls` — the actual gitignored parquet does not exist on this fresh
   worktree, same as every other bars dataset. `rebuild_all()`'s `_bars()`
   closure needs the real file (`bulk.read()` hash-gates it), so a plain
   dispatch-table entry would not have been sufficient; h004 and h001
   already have their own bars-rebuild functions for exactly this reason
   (`rebuild_bars`/`rebuild_lens_bars`).
6. Added `ingest_h07_m5_vantage.rebuild_bulk()` (mirrors `judge_h001.
   rebuild_bulk`'s pattern: locate the existing manifest, rebuild the bytes,
   write to the manifest's own path, hash-verify via `bulk.read()`, raise
   loudly on any mismatch). Extracted `_bars_to_table()` so the ingest-time
   and rebuild-time table construction is the same call, not two copies.
7. In `scripts/rebuild_bulk.py`: loaded `ingest_h07_m5_vantage` as a sibling
   module; added `rebuild_h007_bars` (called from `rebuild_all()` alongside
   the existing two bars-rebuild steps); added `h007_np_liquidity_sweep_v1_1`
   to `_LINEAGE_DATASET`; added the h007 branch to `_events_for_lineage`
   (`LiquiditySweepDetector`, filtered to the sweep event type — the same
   two calls the AC-1 test already makes, reused not reimplemented).
8. Ran the two named tests — both passed. Ran the full suite: one new
   failure appeared, `tests/scripts/test_ac1_engine_parity_np004.py::
   test_ac1_h001_through_h004_reproduce_byte_identically` — its own skip
   condition (`_LINEAGE_DATASET.get(lineage) is None`, a proxy for "is
   h007") stopped meaning that the moment h007 was registered, pulling it
   into the "h001-h004 only" loop and breaking its hardcoded
   `checked == 4`. Fixed by making the skip explicit by lineage name
   instead of relying on the registration-state proxy.
9. Updated `test_rebuild_all_matches_recorded_manifests`'s stale
   `assert len(recorded) == 4` to `== 5` (4 pre-existing + h007), per
   ARCH-NP-005 §3's own acceptance criterion.
10. Added the new regression test, `test_rebuild_all_reproduces_h007_
    manifest_byte_identically` in `tests/scripts/test_rebuild_bulk_s9.py`:
    calls `rebuild_bulk.rebuild_all()` directly (not a re-implementation of
    the byte-comparison — the SAME call whose internal sha-assert is the
    real check), asserts the h007 manifest ref is in the returned list, and
    independently re-hashes via the already-imported `_sha256_file`/
    `BulkStore.path_for` (reused, not duplicated).
11. Full suite: `.venv/Scripts/python.exe -m pytest tests/ -q` → 887 passed,
    1 failed (`test_mt5_csv.py::test_real_ivf_export_ingests_zero_flags`,
    missing external CSV — the same pre-existing, unrelated failure WO-P's
    session documented).
12. `tests/test_kernel_firewall.py` green (8 passed) — no trading vocabulary
    entered `qrf/kernel/**`; nothing in this session touched `qrf/**` at all.
13. Committed to `sprint/NP-S2`; on push, found the remote tip had moved
    (T-058, an Architect boot-artifact commit, `ops/ARCHITECT_BOOT_NP-S2.md`
    only — no conflict with my files). Fetched, rebased cleanly, re-verified
    the two target tests + firewall, re-pushed successfully.
14. Published `ops/aro/handovers/ARCH-NP-005/HANDOVER.md` (ten-section
    shape).
15. This log; committing and pushing to `sprint/NP-S2` now.

## State for the next session
- `scripts/rebuild_bulk.py`'s `rebuild_all()` now succeeds end-to-end
  (verified via the real subprocess-restart test) and covers all 5 lineages
  currently in the journal: h001, h002, h003, h004, h007.
- `rebuild_all()` now depends on the raw Vantage tick source
  (`F:\NeelPrajna\Validation\Stage2\parquet`) being present, in addition to
  the CSV sources it already required — present on this machine, but this
  is a new external-data dependency for anyone running a full rebuild.
- `test_mt5_csv.py::test_real_ivf_export_ingests_zero_flags`'s missing CSV
  gap (documented by WO-P) remains open and untouched — still not this
  instruction's concern.
- No ledger writes anywhere in this session (verified by explicit
  `len(store)` before/after assertions in the new regression test).
- CLAUDE.md's own reference to `docs/coordination/PROTOCOL.md` (line 6-8) is
  stale post-restructure — flagged in this log rather than a DEVQ, since it
  did not block this instruction and someone already updated the rest of
  the file around it (NOTE-NP-004's retirements). Worth a mechanical fix
  next time someone is in `CLAUDE.md` for another reason.

## Definition of Done status
Complete: both named `test_rebuild_bulk_s9.py` tests pass; the new
byte-identity regression test passes; full suite green except the one
confirmed pre-existing, unrelated failure; firewall green; no writes outside
`scripts/**` + `tests/**` (both edited only as required by ARCH-NP-005's own
acceptance criteria — see `HANDOVER.md` §4) + the sanctioned
`ops/aro/handovers/**` + this session log; handover published; branch pushed
to `sprint/NP-S2` (not merged to `main` — P8 is the sprint's single merge,
outside this work order).
