# S7-2 · session log · 2026-07-25 · developer (claude-code)
Instruction: Owner command — DEVQ-016 micro-task (decisiveness strength +
CONTESTED conflict + trial_count v3 + re-derive H-001 belief) · Branch:
claude/arch-007-t0-execution-19e5d2 · Pushed through: (this session, then merge)

CONTEXT: At boot DEVQ-016 was CLOSED on origin/main (ruling: decisiveness strength
`2·|p−0.5|`; CONTESTED stance on decisive disagreement; trial_count v3 gains
"observatory"; re-derive H-001's belief, old state kept). CLAUDE.md rev 4 already
folded in my S7-1 `uv run` finding. Implemented all four items.

DONE (all micro-task items + tests):
- **Decisiveness strength** (belief.py `_decisiveness`, `_stance_and_strength`):
  `strength = 2·|p − 0.5|` ∈ [0,1] replaces the retired p-as-strength. H-001's FAIL
  (p=0.9435) → 0.887; a coin-flip p≈0.5 → ≈0; a degenerate/None p → max. Documented
  as NOT a posterior probability (the Bayesian layer is the deferred ADR).
- **CONTESTED stance** (belief.py + schema enum): newest-decisive-wins WHILE
  decisive verdicts agree; the moment they disagree (PASS after FAIL or vice versa)
  stance = CONTESTED, strength = decisiveness of the newest — the conflict is
  preserved, not resolved by recency. belief stance enum gains CONTESTED (v1,
  unfrozen this sprint); a CONTESTED state must cite a verdict.
- **`BeliefLayer.rederive(family, claim)`**: recomputes the head under the current
  formula and appends a NEW state iff (stance, strength) moved, pointing at the
  prior — idempotent, and the prior state REMAINS in the chain (append-only memory).
- **trial_count v3** (schemas.py, trials.py): `source` enum gains "observatory"
  (forward-only, NOTE-013 shape — v1/v2 never accept it; the two existing S7 scan
  bumps stay v2/human, re-recorded nothing). `TrialCountLedger.bump` writes v3 for
  an observatory source (requires a family). `Observatory.scan` now defaults
  source="observatory", so future scan burdens carry first-class provenance.
- **DEVQ-017** (OPEN, QUESTION, non-blocking): the ruling's marginal-PASS worked
  example (0.098) is arithmetically inconsistent with its own formula (`2·|0.049−0.5|
  = 0.902`); no monotone formula reproduces both 0.098-for-PASS and 0.887-for-FAIL,
  and the H-001 anchor forces `2·|p−0.5|`. Flagged the slip + the fact that the
  formula does not make marginal-SIGNIFICANCE PASSes weak (it makes coin-flip
  results weak); recommend the formula stands (A), with a note that real deflated
  PASSes (p ≤ ~1e-4) are near-maximally decisive anyway.

REAL journal footprint this session (40→41, idempotent): re-derived H-001 belief
HEAD 01KYCHPV8ZNT2F41F8JABD12K2 (REJECTED, strength 0.887), prev_state the retired
p-as-strength state 01KYCFNKCGSYFKWTRYKW54E9C8 (0.9435, kept in chain). Belief chain
now: 0.9435 (retired) → 0.887 (current). No scan re-run (idempotent), so no new
trial_count records — v3 is a forward-only capability, exercised by tests.

Tests added/updated: decisiveness formula values (coin-flip/H-001/deflated/None);
marginal-PASS uses the formula (0.902, with the DEVQ-017 note); thin-evidence PASS
→ ~0; agreeing decisive verdicts stay REJECTED with newest decisiveness; PASS-after-
FAIL and FAIL-after-PASS → CONTESTED; rederive appends when numbers move / is
idempotent / None on missing belief; belief CONTESTED accepted + needs a verdict;
trial_count v3 observatory written at v3 / requires family / rejected by v1+v2.

IN-PROGRESS: — (session closing after merge + push)
BLOCKED-ON: -   NEXT: Architect IVF S7 (scan discipline + no-VIRGIN audit; beliefs
recomputed independently — now includes the decisiveness formula + CONTESTED rule +
the append-only re-derivation chain; ancestry traces; params-reading) + Drill S7
(planted VIRGIN-referencing question + planted belief ignoring a FAIL; a planted
recency-over-conflict belief would now be caught by CONTESTED) + HC caption fix +
visual HC → Owner GO-S7. Ruling wanted: DEVQ-017 (formula stands vs threshold-
relative strength — recommend stands).

Tests: 760 passed (748→+12) · ruff clean · firewall GREEN · journal 41 records
(chain GREEN).
No writes outside permitted paths: qrf/kernel/{belief/belief,records/schemas,
corrections/trials,observatory/scan}.py (code); scripts/{seed_beliefs_s7,observe_s7}.py;
tests/{belief,corrections,records}/ (tests); docs/coordination/inbox/OPEN/DEVQ-017 +
sessions/ (this log); docs/handover/AI_PROJECT_STATE.md via gen_state only.
journal.jsonl appended only through RecordStore.append.
