# QRF — Architect Handover (Fable → next Fable session)
Rewritten 2026-07-25, mid-Sprint-3 close-out · Author: architect (fable)
Audience: the NEXT Architect chat session. Read this file first, then the
files it points to. Chat history is gone; this file + the repo are memory.
PROTOCOL v1.2 duty: rewrite this file at every GO-SN before session end.

## 1. Identity and mission
You are **Fable**, the **Architect** of QRF (Quantitative Research
Framework) — a Python research platform for evidence-first quant trading
research. Three-party team: Owner **Girish** (human, Windows, MINGW64 git
bash, GitHub `girishdomain-gum`, private repo `qrf`, non-native English —
keep language simple, Teaching Standard in docs/reference). Architect
**you** — write instructions/reviews/IVF tools to F:\QRF via Filesystem
access; NEVER developer code; write only on main, only between Developer
sessions. Developer **Claude Code** — works in a git worktree at
`F:\QRF\.claude\worktrees\<branch>\` on `claude/...` branches (NOTE-010),
boots via CLAUDE.md rev 3. Verifier = IVF tools + Owner. Motto:
"prediction first, ontology later"; gate: "Evidence before execution".
Attachments from Girish often arrive EMPTY — ask for plain-text pastes;
screenshots come through.

## 2. Governing documents (all on F:\QRF)
- docs/architecture/ — v1.1 Whiteboard Edition (FROZEN; docx placement by
  Owner may still be pending — remind gently).
- docs/implementation/Implementation_Blueprint_v1.0.md — §1.3
  canonical_bytes NORMATIVE; §2 record catalog; §4 interfaces; §6 errors;
  §7 sprints 1–8.
- docs/implementation/Verification_Framework_v1.0.md — IVF: IND-1..6,
  tolerance classes, per-sprint VC/HC/drills; Go/No-Go = AC+VC+HC+Drill.
- docs/adr/ADR-001..008; docs/coordination/PROTOCOL.md **v1.2** (READ IN
  FULL: session logs/no-console rule, worktree read clause, handover
  duty, push-per-commit, pointers-only relays).
- docs/handover/AI_PROJECT_STATE.md — generated by gen_state; never
  hand-edit.

## 3. Status at this rewrite
- **Sprint 1 CLOSED — GO-S1** (ledger core; genesis + GO-S1 note ids in
  GO-S1.md). **Sprint 2 CLOSED — GO-S2** (detectors seasonality.calendar
  @0.1.0 + classical.rsi@0.1.0, both calibrated 1.0/1.0; VC rev3 GREEN on
  real XAUUSD H1; Drill S2 caught).
- **Sprint 3 (data plane): DEVELOPMENT DONE, close-out IN PROGRESS.**
  ARCH-003 completion report appended; merged+pushed (main 9b2fd86 at
  rewrite time). 124 tests (+37), ruff clean, all AC ✔. Real ingest:
  `xauusd_h1_sample` 504 rows, 0 unexplained flags (MLK 2024-01-15 via
  holidays param), ingest_report 01KYAWHZ77SYPEMPYDY25X8CC1, manifest
  01KYAWHZ6A9X3YZQ2W0BDRFDS1, TRAINING window 01KYAWHZ86ZNDGY4NZNCF4XFY0.
  Journal 12 records, chain GREEN. Gapped-feed seasonality recalibration
  01KYAWJ0REJ7TSM4PRRT18DXD3 (suite .s3, 4 cases). NO VIRGIN yet — that
  is the Owner's act via ARCH-003A.
- **DEVQ-006 CLOSED**: gap rule ratified (C); gap_k=1.0; holidays = call
  parameter; AMENDMENT owed by Developer: `params` object in future
  ingest_report payloads (before any S4 ingest). **DEVQ-007 CLOSED**:
  `__flagged` suffix + `flags` column ratified; suffix reserved; door
  rejection of names containing `__flagged` owed if absent.
- **NOTE-009** (Developer): window_burn schema added (4 data-plane
  schemas). **NOTE-010**: worktree visibility; NOTE-008 pwd item closed.
  **NOTE-011**: S3 session log MISSED — next Developer session owes a
  RETROACTIVE S3-1 log as first deliverable.
- **IVF S3 tools WRITTEN** (this session): ivf/checks/check_s3_dataplane.py
  (Check A rows/prices, Check B flagged-row audit) and
  ivf/checks/drill_s3.py (planted silent repair + tampered quarantine).
  NOT YET RUN at rewrite time.

## 4. Frozen contracts (do not drift)
canonical_bytes §1.3 · EventFrame §4.3 columns · DEVQ-005 DOW contract
(dow.* at first bar of UTC epoch-day, close basis, weekends silent) ·
REV-S2 OBS-4 (MT5 bar time = OPEN; ts = open + timeframe, close basis;
timeframe always explicit) · OBS-5 (RSI amber band ±0.5) · kernel
firewall · window/burn semantics · one-direction rule · NEW: DEVQ-006
gap rule + DEVQ-007 `__flagged` contract (see CLOSED threads).

## 5. Process lessons paid for (NOTES 001–011)
003 writes propagate only via commit+push · 004 status reads require
pull/fetch — and now: the Developer's live work is in the WORKTREE, not
the main folder (010) · 005 push-per-commit, IDs after fetch · 007 ruff
excludes ivf/ · 008 session logs replace consoles · 011 the log rule was
missed once already — check sessions/ at every review. Bug tally:
Architect 4, Developer 2. Expect your own verification tools to be wrong
sometimes; that is a finding (NOTE-002 pattern).

## 6. Owner rhythm
Push after Architect writes (`ARCH:` prefix) → boot Developer with a
one-liner → pull before asking Architect → HC + Go/No-Go. Pointers, not
content. Confirm `(main)` before writing. Sign-offs are verbatim phrases
in GO-SN files. Mid-sprint: Owner may ask you to peek at the worktree
(read-only, PROTOCOL v1.2).

## 7. Immediate next steps (in order)
1. Owner pushes this write window (DEVQ replies, NOTES 010/011, PROTOCOL
   v1.2, this file, IVF S3 tools) with `ARCH:` prefix.
2. Owner boots Developer: "Boot per CLAUDE.md. First: retroactive S3-1
   session log (NOTE-011). Then: DEVQ-006 amendment (ingest_report
   `params`) + DEVQ-007 door rejection if absent. Small session."
3. Owner rebuilds datastore on main (`uv run python
   scripts/ingest_xauusd_s3.py`), exports a FRESH MT5 comparison CSV,
   then runs check_s3_dataplane.py and drill_s3.py (usage in each file's
   docstring). Architect reviews reports → REV-S3.
4. Owner: HC sampling + VIRGIN reserve declaration over the bigger
   export (write ARCH-003A to script it) + Go/No-Go → GO-S3 → REWRITE
   THIS FILE → write ARCH-004 (Sprint 4: screener + costs + SMC,
   Blueprint §7).
5. Standing: Architecture docx placement; Blueprint editorial queue
   (NOTE-001 wording, DEVQ-005, OBS-4, DEVQ-006 rule text §5(2),
   DEVQ-007 suffix §4.2).

## 8. How to verify state yourself (before trusting this file)
Read F:\QRF\.git\refs\heads\main vs refs\remotes\origin\main (pushed?)
and FETCH_HEAD mtimes (stale?); docs/coordination/sessions/ (latest log
— if empty after S3, NOTE-011's retroactive log is still owed);
inbox/OPEN; tail of the newest ARCH file; journal.jsonl tail (count +
chain by eye); and for mid-sprint questions, the worktree under
.claude\worktrees\. NEVER assert "not started" from an unverified lens
(NOTE-004); the main folder alone is NOT the Developer's status
(NOTE-010).
