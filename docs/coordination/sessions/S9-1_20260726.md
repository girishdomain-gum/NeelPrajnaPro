# S9-1 · session log · 2026-07-26 · developer (claude-code)
Instruction: ARCH-009 (Sprint 9 — Second Lens, Rebuild, Enforcement).
Branch: claude/arch-009-t0-execution-59fec3 · Pushed through: commit `b3ea25d`
(T0 + §1 + §2 + §4 foundation). Boot per CLAUDE.md: `git fetch` OK, `origin/main` merged
(branched off f3201ce, the Owner's ARCH-009 data drop). Read PROTOCOL, state file
(stale, pre-S3 — noted), latest session log (S8-2), ARCH-009 FINAL + its two
addenda, GO-S8, DEVQ-018/020, the data-drop provenance sidecars.

## DONE (committed + pushed)

**T0 — `scripts/t0_s9.py`, commit af2900e.** Appended the GO-S8 `note` record
(`01KYDA4KXFBAA7461JJP6H1ZX9`), parented on the GO-S7 note S8's T0 laid down
(`01KYCNVX45TCMS16D22T2NEP3H`). Records the Owner GO ("GO. I agree with closing
Sprint 8."), the Wave-1 hypothesis/placebo/verdict/burn/belief ids, and marks S9
open per ARCH-009. Idempotent. Journal 54→55, chain GREEN.

**§1 — rebuild-bulk for verdict_trades, commit f1350d3.**
- Refactor: extracted `EvidenceBattery.trades_table(outcomes)` (static) as the
  SINGLE construction of the `verdict_trades.*` table; `_trades_manifest` (write
  path) and the new rebuild path both call it, so a rebuild is byte-identical BY
  CONSTRUCTION (never a parallel implementation — the §1 binding).
- `scripts/rebuild_bulk.py`: for every verdict with a `trades_manifest`,
  reconstructs the exact setup events per lineage (dispatch: h001 = SMCFVGDetector;
  h002 = intra-week filter; h003 = Monday markers — the SAME builders the judges
  used, loaded by file-path since scripts/ is not a package), re-runs the recorded
  experiment via `EvidenceBattery.evaluate` (the placebo/replay entry — no burn),
  rebuilds the table, writes it to the manifest's path, and ASSERTS rebuilt
  sha256 == manifest.file_sha256 (loud SystemExit on any mismatch; a rebuild that
  "mostly matches" is a fabrication). Bars parquet rebuilt first (reuses
  judge_h001). An unregistered lineage is a loud failure, never a silent skip.
- Proven on all three on a clean bulk root: h001 (654 rows) / h002 (637) /
  h003 (28) all sha assert-equal. Appends nothing (asserted).
- Tests `tests/scripts/test_rebuild_bulk_s9.py`: matches-recorded-manifests;
  refuses-unknown-lineage; **byte-stable across process restarts** (two fresh
  interpreters both reproduce the recorded manifests). 3 passed.

**§2 — placebo_method under the seal, commit 4381501.**
- Optional v2 hypothesis field `placebo_method` (schema `_validate_hypothesis_v2`,
  additive → Wave-1 H-002/H-003 grandfather unchanged, NO migration). Registry
  `_resolve_placebo_method` validates membership against `placebo.PLACEBO_METHODS`
  (the judge's OWN dispatch set — one source of truth) and REFUSES an unknown
  method; the content-hash seal covers it (verify_frozen catches a method edit);
  it requires a v2 hypothesis (like observatory_ancestry).
- `PlaceboBattery.run` REFUSES a requested method that disagrees with the sealed
  `placebo_method` (SchemaViolation naming both); a grandfathered record (no
  sealed field) runs with the caller's method exactly as Wave-1 did.
- Tests: `tests/protocol/test_hypotheses_placebo_method_s9.py` (seal, unknown
  refusal, requires-v2, verify_frozen tamper, grandfather shape) +
  `tests/battery/test_placebo_method_seal_s9.py` (judge mismatch/agree/grandfather).
  9 passed.

Regression: battery + protocol + records + graduation + firewall suites GREEN;
ruff clean on all changed files; journal chain GREEN (55). VIRGIN untouched.

## §4 FOUNDATION — DONE this session (commit b3ea25d)
The Owner (2026-07-26) chose Option 1 ("I build, Owner runs the VIRGIN gate"),
ratified 0.30 trailing, and added binding §4.2 display rules + two §4.3 flags
(→ DEVQ-022). All the §4 work that does NOT cross the Owner gate is now done:

- **Ingest (`scripts/ingest_lens_feeds_s9.py`)** — both feeds ingested raw
  (storage is not computation): `xauusd_h1_primary_full` (Winprofx, 11857 bars,
  ingest_report `01KYDC8XGXE7...`) + `xauusd_h1_secondfeed` (Exness XAUUSDm,
  11844 bars, `01KYDC8XMJ...`), both PASS / 0-flagged. New CSV schema is
  adapter-compatible via `IVF_S2_COLUMN_MAP`. primary_full's first ts equals the
  existing `xauusd_h1_full` window start (1704160800e9) — same Winprofx feed,
  extended to 2026. Idempotent.
- **Split tool (`scripts/declare_virgin_2025_s9.py`)** — the Owner's next action.
  Reads the ingested primary_full, computes the 2025 extension (5920 bars,
  ts≥1735689600e9), and DISPLAYS boundary ts (server wall-time AND epoch),
  TRAIN/VIRGIN counts, and fraction BEFORE prompting `DECLARE VIRGIN` (the
  Owner's binding requirement — types against numbers). 0.30 → **4144 TRAINING /
  1776 VIRGIN**; last-TRAINING ts 1757682000e9 (2025-09-12 13:00), first-VIRGIN
  ts **1757685600e9** (2025-09-12 14:00, epoch 1757685600). Verified the display
  + abort-on-mismatch path with a declined phrase (journal unchanged). Idempotent.
- **Pre-registration note SEALED (`scripts/prereg_second_lens_s9.py`)** — journal
  note `01KYDCNRM4G7VTE5JRPY4D84K3` (60 records), parented on the two
  ingest_reports so its chain position PROVES it precedes any overlap (DEVQ-020
  "the whole point"). Fixes, before any peek: (1) clock-era alignment procedure
  (piecewise EU-DST eras 2024-03-31/10-27, 2025-03-30/10-26; per-era integer-hour
  shift from {0,±1,±2,±3} by max shared-count; runner-up within 5% → STOP+DEVQ),
  (2) OHLC tolerance |Δo|,|Δc|≤0.50 / |Δh|,|Δl|≤0.75 USD/oz (generous → bias
  toward agreement, so a LOW rate is the conservative-direction finding) +
  interpretation threshold agreement_rate≥0.95, (3) BOTH reserves excluded by ts
  range: 2024-VIRGIN [1726128000e9,1735689600e9), 2025-VIRGIN
  [1757685600e9,1767225600e9+1). tier=BROKER recorded (→ second_lens source_name).
- **DEVQ-022 (BLOCKER, H-004 only)** — window topology (non-contiguous
  2024+2025 training; recommend multi-window schema v3 with drop-and-count + seam
  folds, fallback 2025-only) + min_n (72 calendar Mondays, ~56 expected surviving
  at H-003's 0.778 rate → propose **min_n = 45**; 80 would doom to INSUFFICIENT).

## BLOCKED-ON (hand-off, cannot be crossed in a non-interactive Dev session)
1. **Owner** runs the typed-phrase 2025 VIRGIN designation (command below).
2. **Architect** rules DEVQ-022 (H-004 window topology + final min_n) before the
   H-004 YAML can seal.

Owner command (git-bash, from the repo root — designates the 2025 reserve after
reviewing the displayed numbers, then type exactly `DECLARE VIRGIN` when prompted;
anything else aborts and writes nothing):
```
cd /f/QRF/.claude/worktrees/arch-009-t0-execution-59fec3 && PYTHONPATH=. .venv/Scripts/python.exe scripts/declare_virgin_2025_s9.py
```
(If the worktree has no `.venv`, use the main one: replace `.venv/Scripts/python.exe`
with `/f/QRF/.venv/Scripts/python.exe`. Expected output: a boxed summary of the 2025
extension, then the DECLARE VIRGIN prompt. On success it prints two new window ids
and `journal verify ok=True n_records=62`.)

## REMAINING (a follow-up Dev session, after 1 + 2 above)
§4.1: build+run the clock-era alignment + overlap engine (per the sealed
pre-registration), append the first `second_lens` record (source_name carries
tier=BROKER; agreement_summary.notes carries the per-era shift tables). §4.3:
register + judge H-004 per the DEVQ-022 ruling (placebo-first, entry_time_shuffle
sealed via §2). Then sprint close: full suite, session log, merge.

## State at session end
Commits af2900e (T0) · f1350d3 (§1) · 4381501 (§2) · 736d095 (S9-1 log) ·
b3ea25d (§4 foundation), all pushed to origin/claude/arch-009-t0-execution-59fec3.
Journal **60 records, chain GREEN**; both VIRGIN reserves untouched (the 2025 one
not yet designated — awaiting the Owner). Firewall GREEN; ruff clean on all
changed files. §1 + §2 test suites GREEN (12 new tests). No writes outside
permitted paths (scripts/, qrf/ production + tests, journal via
RecordStore.append only, coordination inbox/sessions, this log). NOT merged to
main — sprint is incomplete by design (§4.1/§4.3 blocked on Owner + Architect).
