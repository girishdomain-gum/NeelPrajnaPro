# S4-1 · session log · 2026-07-25 · developer (claude-code)
Instruction: ARCH-004 (Sprint 4 — Screener + Costs + SMC)
Branch: claude/arch-004-t0-execution-fe57c0 · Pushed through: (this session)

DONE:
- T0: GO-S3 note appended (01KYB5ARJPK3YK0AKCE9FP7DAH), parent GO-S2 note;
  "S4 T0" committed. Everything below descends from it.
- Screener (qrf/trading/simulator/screener_vbt.py): vectorbt sweep → shortlist
  (parquet+note declaring metric/thresholds) + exact-grid-size trial_count in one
  code path; TRAINING/EXPLORATION-only (WindowLedger.check_screenable; VIRGIN →
  ContaminationError); no verdict / no window_burn (AST call-site audit test).
- TrialCountLedger (qrf/kernel/corrections/trials.py) + trial_count schema (§2):
  bump/total, monotone, source enum, generator inheritance. Kernel-clean.
- Cost models (qrf/trading/utility/cost_models.py + configs/venues.yaml):
  xauusd_retail_median; apply gross→net deterministic, hand-computed to the cent.
- SMC detector (qrf/trading/concepts/smc/detector.py): smc.fvg.bull/bear +
  smc.order_block.bull/bear wrapping smartmoneyconcepts==0.0.27, with a causal
  knowability wrapper; hand-planted fixtures (truth/silence/insufficient),
  anti-hindsight property green, version pin recorded. Registered + calibrated
  through the real journal (scripts/bootstrap_smc_s4.py).
- Acceptance (scripts/screen_s4.py): AC1 real 500-grid over xauusd_h1_sample
  TRAINING in ~12s → shortlist + trial_count(500) in the real journal, gross≠net
  visible; AC2 random no-edge 500-grid → EMPTY shortlist (scratch + unit test).
- Completion report appended to ARCH-004; gen_state run.

IN-PROGRESS: — (session closing after merge + push)
BLOCKED-ON: -   NEXT: Architect IVF S4 checks + Drill S4 + HC tool rev 5;
Owner visual HC on SMC zones → GO-S4. DEVQ-008/009/010 await ratification
(non-blocking; proceeded on recommendations).
Tests: 182 passed (+49) · ruff clean · firewall GREEN · journal 25 records,
chain GREEN, head 134a60ef0e61

DEVQs filed (all QUESTION, non-blocking): DEVQ-008 cost-model instrument kind ·
DEVQ-009 screening metric definition · DEVQ-010 SMC library version + zone
causality reconciliation. Each proceeds on a recommended option; reversal is
additive, no rework blocked.

Note on AC1: on the 504-bar sample under min_trades=30 the shortlist admits 0 —
honest for a small sample; the admit path is exercised by unit tests on trending
data (screener admits 180/500 there). Logged rather than tuning thresholds to
force an admit.
