# S4-2 · session log · 2026-07-25 · developer (claude-code)
Instruction: REV-S4 micro-tasks (pre-GO-S4)
Branch: claude/arch-004-t0-execution-fe57c0 · Pushed through: (this session)

DONE (three REV-S4 micro-tasks):
- MT1 — venues.yaml freeze test (DEVQ-008 ruling). tests/utility/test_venues_freeze.py:
  cited cost-model names pinned to a snapshot (edit a cited entry → CI red →
  mint a new name); every cost_model cited in the real journal must be in FROZEN;
  guard test proves xauusd_retail_median really is cited.
- MT2 — screener derives + records a seed (F-4). screener_vbt.py: run() now records
  a non-null seed + seed_source ("derived"/"explicit"); when no seed is passed it is
  DERIVED deterministically from the run identity (sha256 of datasets/events/grid/
  cost_model/window/lineage/thresholds → 63-bit int). Seed lives only in the note;
  the shortlist parquet does not depend on it. +3 tests.
- MT3 — --rebuild-bulk for detector/screener datasets (F-5). Refactored the sweep
  into a pure Screener.compute_ranking (no journal writes); scripts/screen_s4.py
  --rebuild-bulk regenerates the xauusd_h1_sample_smc_fvg_events (smc.fvg detect)
  and screener_shortlist (ranking) parquet from the manifests, hash-verified,
  appending NOTHING. Verified: deleted both parquet, rebuilt from scratch, both
  hash-matched their existing manifests (events sha 7757b84…, shortlist sha
  5e60ec6…); journal unchanged at 25.

IN-PROGRESS: — (session closing after merge + push)
BLOCKED-ON: -   NEXT: Architect re-run of check_s4_screener.py; visual HC
(sample_s4_zones.py → IVF_S4_HC_Zones.mq5 → PNGs → "HC-S4 PASS"); Owner Go/No-Go
→ GO-S4.
Tests: 188 passed (+6) · ruff clean · firewall GREEN · journal 25 records,
chain GREEN (unchanged this session — rebuild + reads only, no appends).

Heads-up for GO-S4 (see NOTE-013): the seed fix is code-forward-only. The one
shortlist note already on main (01KYB7X30…) predates the fix and records
seed=null; append-only means that value is immutable, and screen_s4.py's
idempotency guard blocks a duplicate seeded shortlist by design. So
check_s4_screener.py rev 3 still ambers once on C.seed for that historical note
(red=[], FVG recompute 105/105 exact, all else green). The amber is a pre-fix
artifact, not an unfinished task; the seed contract is enforced in code + tests
for every future run. NOTE-013 asks the Architect to read that single amber as
accepted (or make the check audit only the latest shortlist — an IVF-side change).

No writes outside permitted paths: qrf/** + tests/** + scripts/** (code), one new
NOTE, this session log. No journal appends; datastore/bulk is gitignored (rebuilt,
not committed).
