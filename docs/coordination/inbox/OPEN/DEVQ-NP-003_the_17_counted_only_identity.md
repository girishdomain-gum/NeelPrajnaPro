# DEVQ-NP-003 · BLOCKER (deliverable 6 only) · Sprint NP-S1 · 2026-07-30
**Status: roster question ANSWERED (Owner, this chat, 2026-07-30) — Option A, via `F:\NeelPrajna\knowledge_base\kb.json`. Registration stays held behind the NEW finding raised while resolving this — see `DEVQ-NP-004_h07_h08_gate_provenance_misattribution.md`.**
Author: developer (claude-code), worktree `claude/np-adr-008-liquidity-sweep-7aa72b`
Refs: Execution Plan v2.0 §4 deliverable 6 / AC-5; §7 (NP-S3, "Family migration (the 17)"); `docs/archive/planning/NP_INTEGRATION_EXECUTION_ROADMAP.md`; `docs/automation/NeelPrajnaPro_Automation-v1.0.md` ("Owner touches: the 17-hypothesis ruling"); `docs/scientific_model/NeelPrajnaPro_Scientific_Model-v1.0.md` §8.1; NP-ADR-008 §8; `ops/DEVELOPER_BOOT_NP-S1_RESUME.md` ("THE ORDERING TRAP"); `docs/coordination/inbox/CLOSED/DEVQ-NP-001_h07_export_definition_mismatch.md` (prior hold on this exact deliverable, endorsed by the Architect 2026-07-30).
Tag: specification-gap (forward dependency, not H-07-definition family — not covered by the "no new DEVQ on the H-07 definition question" exception)

## Summary (one line)
Deliverable 6 ("remaining 17 founding hypotheses registered as counted attempts") has no discoverable, ruled identity for what those 17 actually are — and the Execution Plan's own text says the Owner rules on "the 17" (subset, priority, n-floors) **at NP-S3, informed by NP-S1's own comparison report**, which does not exist yet. Registering 17 fabricated lineage names now to satisfy the ordering trap would violate P2 (registration before experimentation) and P6 (reproducible evidence) on the fabricated 17, even though it would count correctly toward the α-budget.

## What I found
1. No file in this repository enumerates 17 (or 18) named NeelPrajna hypotheses with H-01..H-18-style identifiers. Grepped exhaustively (`H-0[1-9]`, `H-1[0-8]`, "founding hypothes*", "the 17", "18 founding") across `docs/`, `ops/`, `qrf/`, `datastore/journal/journal.jsonl` — the only 18/17 references are the *count* (Scientific Model §8.1: "The 18 founding hypotheses register as ≥18 counted attempts... every prior bespoke sweep that can be reconstructed from `np_*` artifacts shall be counted honestly against the family, per QRF-ADR-011"), never a roster.
2. `configs/hypotheses/` contains only `h001`–`h004` (this Kernel's own native hypotheses, unrelated to NeelPrajna). No `h007` or neelprajna-family YAML exists yet.
3. **The Execution Plan's own §7 (NP-S3) says the identity question is not yet ruled:** *"Opens with the Owner's ruling on the 17, informed by NP-S1's comparison report: subset, priority, n-floors."* `docs/automation/NeelPrajnaPro_Automation-v1.0.md` echoes this: *"Owner touches: the 17-hypothesis ruling."* Both place the ruling **after** NP-S1's deliverable 5 (the comparison report) exists — but deliverable 6 asks me to register the 17 **during** NP-S1, before that report is written. I cannot find a resolution of this forward reference in NP-ADR-008 (which rules the *count*, 19, but not the *contents* of the 17) or anywhere else on the ratified record.
4. My immediate predecessor session raised this exact concern and held deliverable 6 for it (`DEVQ-NP-001` §"How this blocks": *"Deliverable 6 (register the 17 as counted-only ledger entries) is independent but I am holding it too... Silence binds no one"*), and the Architect's reply explicitly endorsed the hold (*"Your hold on deliverable 6... is endorsed — correct instinct... Nothing registers until the ADR rules it"*). NP-ADR-008 has since ratified the **pricing** (19 trials → p<0.00263) but I can find no ruling that also supplies the 17's **identities** — the hold's original reason (pricing) is resolved, but a second, independent reason (identity) was latent in the same hold and remains open.
5. Mechanically, `TrialCountLedger.bump()` (`qrf/kernel/corrections/trials.py:48-108`) requires a `lineage` string per call and is not itself gated on a full hypothesis YAML — so deliverable 6 could technically be satisfied by 17 `bump()` calls with *some* 17 lineage strings without a full `HypothesisRegistry.register()` per entry ("ledger entries only" is consistent with this reading). But I still need genuine lineage identities to write honest records — Scientific Model §8.1 requires counting "every prior bespoke sweep that **can be reconstructed** from `np_* ` artifacts," which implies a specific reconstruction, not 17 arbitrary placeholders.

## Why I am not guessing
Constitution P2 ("every threshold, method, null construction, and success criterion is sealed before the judging data is examined") and P6 ("a claim that cannot be re-derived from raw records and normative texts is not knowledge") both bind here. Fabricating 17 lineage names/theses would create 17 permanent, append-only ledger records with no evidentiary basis — worse than leaving them unregistered, because P5 means they can never be silently deleted if wrong, only superseded by new records pointing at the mistake.

## What this blocks
- **Deliverable 6 / AC-5 only.** Not deliverables 1, 2, 3 (the two sealed H-07 registrations), or the window designation — none of those depend on the 17's identity, and I am proceeding with them.
- **Transitively blocks deliverable 4 (the real `EvidenceBattery.run()`) and therefore 5, by the ordering trap already documented in `ops/DEVELOPER_BOOT_NP-S1_RESUME.md`:** `deflate_family` totals `trial_count` records at judgment time; running the Battery with only 2 of 19 trials registered would price the verdict at α≈0.025 instead of the ratified p<0.00263 — a bar roughly ten times too loose. **I will not run the Battery until this is resolved**, even though the detector, ingestion, and the two H-07 registrations may all be finished first.

## Options (for the Architect to rule, Owner to ratify if it touches the seal)
- **A — Reconstruct the 17 now, from `np_*` artifacts, as this session's own deliverable.** I have read-access to the sibling `F:\NeelPrajna` tree (confirmed working this session) and could attempt to reconstruct prior bespoke sweep/screen history (e.g. `knowledge_base/kb.json`, any `np_hypothesis_zero.py` output, the EntryGates B1–B6/T1–T9 roster referenced in `docs/books/book-a-neelprajna`) into 17 honestly-provenanced counted-only lineages. Risk: this is exactly the judgment call NP-S3's text reserves for the Owner, informed by a report I haven't written yet — doing it now may pre-empt that ruling.
- **B — Register a single placeholder/lump-sum trial_count entry for now** (e.g. one `bump(n=17, ...)` under an explicitly-provisional lineage such as `neelprajna_founding_pending_np_s3_ruling`), spending the family budget honestly today and re-keying to 17 individually-identified lineages at NP-S3 once the Owner rules — P5 (append-only, corrections point at old records) would let this be superseded cleanly later. Risk: `_trial_belongs_to_family` and downstream trial-accounting reports would show one lump entry instead of 17 named ones until NP-S3.
- **C — Defer deliverable 6 (and therefore the Battery run) to a follow-up session/DEVQ answer, and this session delivers only what's unblocked: the detector (AC-1/AC-2), the M5 ingestion, the window designation, and the two sealed H-07 registrations (deliverable 3).** No ordering-trap risk, no fabrication risk, but the sprint's headline result (the Battery verdict, AC-3/AC-4) waits on this answer.

I have no authority to choose; recommend **C** for this session (finish everything genuinely unblocked, stop cleanly at the real boundary) with **A or B** as the Architect's call for how deliverable 6 itself gets satisfied.

## What I have done / will do while this is open
Proceeding now with: `qrf/trading/concepts/neelprajna/liquidity_sweep.py` (deliverable 1, AC-1/AC-2), the M5 tick→bar ingestion into BulkStore, the WindowLedger TRAINING designation, and the two sealed H-07 registrations (deliverable 3a/3b — prediction claim + E2 existence claim). **Not** doing: deliverable 6, the AC-4 mapping seal (depends on the run), the `EvidenceBattery.run()` (deliverable 4), or the comparison report (deliverable 5) — all held behind this DEVQ per the ordering trap. Silence binds no one.

---
## REPLY · Owner · 2026-07-30 (this chat)
**Roster question answered:** *"the roster is at F:\NeelPrajna\knowledge_base\kb.json; the 17 are enumerable with genuine provenance, so Option A is available and Option B's placeholder is unnecessary."* This is Option **A** — reconstruct the 17 from `kb.json`'s H-01..H-18 roster (minus H-07) with genuine provenance, not a lump-sum placeholder.

**Read in full** (see `DEVQ-NP-004_h07_h08_gate_provenance_misattribution.md` for the complete record): `kb.json` gives all 18 founding hypotheses (H-01..H-18) with `hypothesis`, `lineage` ("#00N Gate M" or similar), `executable_definition`, `status`, `maturity`, and an `evidence` array. H-07 is confirmed as the genuine 324-trade population lineage this sprint has worked with throughout. The other 17 (H-01..H-06, H-08..H-18) are the roster for deliverable 6.

**But the Owner's same message raised a NEW, more severe finding while pointing at this roster:** *"if H-07's gate is LiquiditySweepGate.mqh, then NP-ADR-008's provenance section needs an appended correction, and one of the 17 (H-08) is the hypothesis v1.0 actually described... before registering anything further, resolve the identity question."* Verified directly against `T3_SweepFVGGate.mqh`'s own header and git history — confirmed with high confidence (full writeup: `DEVQ-NP-004`). **Registration of the 17 (including H-08 specifically) stays held behind DEVQ-NP-004, not this DEVQ.** This DEVQ's own original question (roster identity/source) is answered and this file is otherwise closed.
