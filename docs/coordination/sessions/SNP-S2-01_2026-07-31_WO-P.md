# Session SNP-S2-01 · Sprint NP-S2 · 2026-07-31
Role: **Developer**, worktree `sprint-np-s2-wo-p-e3177d`, branch `sprint/NP-S2`.
Instruction: `ops/ARCH-NP-004_WO-P_execution_parity.md` (incl. §9 addendum) via
`ops/DEVELOPER_BOOT_WO-P.md`.

## Outcome (one line)
WO-P complete: `ExecutionSpec` now supports a per-trade, event-sourced stop and
an R-multiple target (ARCH-NP-004 §4.1/§4.2); `engine_version` bumped
`s5.1` → `s5.2`; AC-1 through AC-7 all pass; handover published at
`ops/aro/handovers/WO-P/HANDOVER.md`.

## What happened
1. `git fetch origin` — the worktree's branch (`claude/sprint-np-s2-wo-p-e3177d`)
   was already a descendant of `sprint/NP-S2`'s tip (`8bf0e28`, one commit
   ahead at `908a3f0`), and `origin/main` matched local `main` exactly. No
   rebase/merge needed; a prior session had already staged the worktree.
2. Read, in order: `ARCH-NP-004` (incl. §9), `PFR_NP-S2.md` (confirms WO-P
   unblocked — B1–B3 don't touch it), `SPRINT_STATE_MACHINE_v1.1.md` §9–§10,
   `qrf/trading/simulator/engine.py` + `fills.py` (the code, not a description
   of it, per the boot doc).
3. Explored (Explore agent + direct reads) the registry (`hypotheses.py`,
   `schemas.py`), the EventFrame closed schema (`qrf/kernel/instruments/
   base.py` — `level`/`zone_hi`/`zone_lo` are the only float64 columns), the
   sealed hypothesis/verdict/manifest records in the real journal, and
   existing test conventions (`test_engine.py`, `_micro_scenario.py`,
   `test_rebuild_bulk_s9.py`, `judge_h007_prediction_s1.py`) — all before
   writing any code.
4. **Design (recorded fully in the handover §4):** per-trade stop carried on
   an EventFrame column named by a new `ExecutionSpec.event_stop_column`
   (restricted to `{level, zone_hi, zone_lo}`, so AC-5's "the EventFrame
   cannot supply it" is a real, checkable refusal); R-multiple target via
   `target_r_multiple`. Both resolve to a per-trade *effective*
   `stop_offset`/`target_offset` inside `EventEngine.simulate`, then flow
   through the **unmodified** `fills.resolve_exit` — `fills.py` needed zero
   logic changes, and AC-1 holds by construction (no arithmetic on the legacy
   path) as well as by test.
5. Implemented: `qrf/trading/simulator/engine.py` (new `ExecutionSpec` fields
   + validation + docstring; `simulate()`'s per-event resolution; version
   bump), `qrf/kernel/records/schemas.py` (`_validate_execution` — the three
   AC-5 refusals + two mutual-exclusivity guards + an `isfinite` fix to the
   pre-existing `stop_offset`/`target_offset` check, which previously let
   `inf` through), `qrf/kernel/protocol/hypotheses.py` (`_EXECUTION_KEYS`).
6. `uv sync` (fresh worktree, no `.venv` yet — one-time; direct `.venv/Scripts/
   python.exe` invocation from then on, per the environment note).
7. Wrote tests: AC-2/AC-3/AC-4 + `ExecutionSpec`-level AC-5 mirrors in
   `tests/simulator/test_engine.py`; registry-level AC-5 refusals in
   `tests/protocol/test_hypotheses.py`; a new `tests/scripts/
   test_ac1_engine_parity_np004.py` proving byte-identical reproduction of
   h001–h004 and the judged h007 registration (the unjudged 2nd h007
   registration has no verdict to compare — checked its execution dict
   parses unaffected instead).
8. Ran the full suite. Found two failures, both confirmed pre-existing and
   unrelated: `test_mt5_csv.py::test_real_ivf_export_ingests_zero_flags`
   (missing external CSV) and `test_rebuild_bulk_s9.py` (`scripts/
   rebuild_bulk.py` has no dispatch entry for the h007 lineage and now
   fails on ANY invocation, since the h007 verdict is in the journal but
   was never added to `_LINEAGE_DATASET` — confirmed via untouched `git diff`
   on that file). Wrote `docs/coordination/notes/
   NOTE-NP-003_rebuild_bulk_missing_h007_lineage.md` rather than fixing it
   (`scripts/` is outside WO-P's write scope, `qrf/**` + `tests/**`); my own
   AC-1 test drives the same underlying pipeline pieces directly instead of
   the broken orchestrator. Fixed one REAL regression my own change caused:
   `tests/battery/test_battery.py` asserted the literal `"engine.s5.1"` —
   updated to compare against `EventEngine.engine_version` instead of a
   frozen string, since the bump is intentional and spec-mandated.
9. `tests/test_kernel_firewall.py` green (AC-7) — no trading vocabulary
   entered `qrf/kernel/**`, no `qrf.trading` import.
10. Published `ops/aro/handovers/WO-P/HANDOVER.md` (ten-section shape).
11. This log; committing and pushing to `sprint/NP-S2` now.

## State for the next session
- **AC-1 through AC-7 all pass.** Full suite green except two confirmed
  pre-existing, WO-P-unrelated failures (`NOTE-NP-003` + the `test_mt5_csv.py`
  missing-CSV case) — see `HANDOVER.md` §5/§7 for exact commands.
- `engine_version` is now `"engine.s5.2"`. Any future work touching verdict
  provenance stamps should expect this, not `"s5.1"`.
- No ledger writes anywhere in this session (P2 rule) — verified by explicit
  `len(store)` before/after assertions in the new AC-1 tests, not just
  asserted in prose.
- `scripts/rebuild_bulk.py`'s missing h007 dispatch (`NOTE-NP-003`) is
  unresolved and will keep failing `test_rebuild_bulk_s9.py` for anyone who
  runs it — not blocking, but real; someone should decide whether it becomes
  a DEVQ.
- No detector was wired to actually populate a per-trade stop column — this
  work order built the capability only, per its own non-goals (no H-007
  re-registration, no R6 collection).

## Definition of Done status
Complete: AC-1–AC-7 pass; firewall green; no writes outside `qrf/**` +
`tests/**` + the sanctioned `docs/coordination/notes/` + `ops/aro/handovers/**`
+ this session log; handover published; this log written; branch pushed to
`sprint/NP-S2` (not merged to `main` — P8 is the sprint's single merge,
outside this work order).
