# Session SNP-M1-01 · ARCH-NP-009 · 2026-07-31
Role: **Developer**, worktree `neelprajnapro-sprint-np-s1-a8171d`, branch
`maint/gen1-cleanup` (branched from `origin/main@9df8499`, cut after the
`maint/adr-registry` merge, as the instruction required).
Instruction: `ARCH-NP-009` (CLAUDE.md rev 5, NOTE-NP-005, and Gen-1 cleanup —
transcription of two Architect-authored artifacts plus three Developer fixes).

## Outcome (one line)
T1-T5 complete: CLAUDE.md replaced with rev 5 and NOTE-NP-005 transcribed verbatim;
`rebuild_bulk.py`'s docstring repointed at this repo's own venv; the stale
`IVF_S2_XAUUSD_PERIOD_H1.csv` path repointed at its real archive location; F-27 sweep
run (method validated first) with every hit reported and none in scope left unfixed;
full suite **888 passed, 1 warning in 148.51s** — the first time this repository's
suite has been genuinely green, 0 failed, 0 skipped, not a "pre-existing failure"
carried forward; firewall green (8 passed); handover published; branch pushed, not
merged.

## What happened
1. `git fetch origin`. Confirmed `maint/adr-registry` was already merged into
   `origin/main` (`9df8499` — visible in the merge commit's own summary). `main`
   was checked out in the sibling worktree, so branched directly off
   `origin/main` (`git checkout -b maint/gen1-cleanup origin/main`) rather than
   the instruction's literal `git checkout main` step — identical resulting
   tip, no local `main` touched, same accommodation used in the two prior
   ARCH-NP-007/008 sessions in this worktree.
2. T1: read the existing `CLAUDE.md` (rev 4, the tool requires a prior read
   before an overwrite), then replaced it wholesale with the supplied rev-5
   text — no rewriting, no reformatting.
3. T2: wrote the supplied NOTE-NP-005 text verbatim to
   `docs/coordination/notes/NOTE-NP-005_rebuild_bulk_documented_invocation_binds_archived_origin.md`.
4. T3: edited only `scripts/rebuild_bulk.py`'s module docstring — swapped both
   `F:/QRF/.venv/Scripts/python.exe` examples for `.venv/Scripts/python.exe`
   and added the one sentence the instruction specified, citing NOTE-NP-005.
   Confirmed by `git diff` that zero executable lines changed.
5. T4: found the real CSV at `docs/archive/gen1/artifacts/
   IVF_S2_XAUUSD_PERIOD_H1.csv` (505 lines, correct OHLC+RSI shape). Read
   `test_real_ivf_export_ingests_zero_flags` and confirmed it asserts nothing
   about the file's location — only about what ingesting it produces.
   Repointed the `REPO_ROOT / "IVF_S2_XAUUSD_PERIOD_H1.csv"` constant at the
   archive path (the Architect's stated preference, and independently what
   the test's own assertions supported — reasoning recorded in
   `HANDOVER.md` §4). `.venv` was absent in this fresh worktree; ran
   `uv sync` once, then `.venv/Scripts/python.exe -m pytest
   tests/adapters/test_mt5_csv.py -q` → 16 passed, before trusting the fix in
   the full suite.
6. T5: validated the F-27 sweep method before trusting a clean result — ran
   each of the three grep patterns against a case I knew would match first
   (NOTE-NP-005's own just-written text for `F:/QRF`; the prior
   `SNP-S2-02` session log's mention for `coordination/PROTOCOL`) — both
   surfaced as expected. Then ran the three real sweeps
   (`coordination/PROTOCOL`, `F:/QRF`, `QRF project`, each excluding
   `docs/archive`). 4 + 26 + 2 = 32 hits total; classified every one.
   None inside T1/T3 scope needed fixing beyond what T1/T3 already did — the
   4 `coordination/PROTOCOL` hits and 2 `QRF project` hits are all
   historical/Architect-authored records correctly quoting the old defect,
   not live stale references; the 26 `F:/QRF` hits are 2 intentional
   quotations inside NOTE-NP-005 itself plus 24 lines across 13 other
   `ivf/`/`scripts/` files carrying the identical bug class NOTE-NP-005
   describes — out of T3's single-file scope, listed in full in
   `HANDOVER.md` §7 rather than fixed.
7. Ran the full suite: `.venv/Scripts/python.exe -m pytest tests/ -q -rs` —
   exit 0, all dots, no failures, no skips visible, but no final "N passed"
   summary line appeared in this session's captured output. Diagnosed before
   assuming success: re-ran the identical test selection without `-q`
   (`pytest tests/ -rs`) and got the summary line cleanly:
   **`888 passed, 1 warning in 148.51s (0:02:28)`** — 888/0/0, matching the
   instruction's expected shape exactly. Isolated the discrepancy to `-q`
   mode specifically (verbose `-v` runs on a subset printed the summary
   normally both times); recorded as an environment/tool quirk in the
   handover rather than assumed away, per CLAUDE.md rev 5's own new rule
   about not reporting an assembled count.
8. `tests/test_kernel_firewall.py -v` → 8 passed — firewall green, confirming
   nothing in T1-T4 introduced trading vocabulary into `qrf/kernel/**` (none
   of the four changed files are under `qrf/` at all).
9. `git diff --stat origin/main...HEAD` — exactly the four files named in the
   instruction's T1-T4, 136 insertions, 16 deletions, nothing else touched.
10. Published `ops/aro/handovers/ARCH-NP-009/HANDOVER.md` (ten-section shape).
11. This log; committing and pushing to `maint/gen1-cleanup` now.

## State for the next session
- This is the first fully green run of the suite in this repository's
  history under this branch model: 888 passed, 0 failed, 0 skipped. No
  failure was carried forward as "pre-existing" this time.
- The `F:/QRF`-binding bug NOTE-NP-005 documents is fixed only in
  `scripts/rebuild_bulk.py`'s docstring (T3's named scope). 13 other files
  across `ivf/checks/`, `ivf/human/`, and `scripts/*_s8.py`/`*_s9.py`/
  `*_s10.py` carry the identical pattern, full list in `HANDOVER.md` §7.2 —
  open, not a DEVQ, flagged for a future instruction if a repo-wide sweep is
  wanted.
- `CLAUDE.md` is now rev 5: points at the correct roles document, states the
  branch model correctly (no merge-to-main in the Developer's own DoD), and
  carries the F-26/F-27/F-28 rules this and the two prior ARCH-NP-007/008
  sessions each triggered once.
- Branch `maint/gen1-cleanup` is pushed, four commits ahead of `origin/main`,
  not merged — per the instruction, merge was never in scope.
- A pytest `-q`-mode summary-line capture quirk in this tool's Bash
  environment is recorded in `HANDOVER.md` §10 — not a repository defect,
  but worth knowing before assuming a `-q` run without a visible summary
  line means something went wrong.

## Definition of Done status
Complete: T1-T5 done; full suite green (`888 passed, 1 warning in 148.51s`,
runner's own quoted line, not an assembled count); kernel firewall green
(8 passed); no writes outside the four files T1-T4 name (plus this
handover/session-log pair, both sanctioned); handover published; branch
pushed to `maint/gen1-cleanup` (not merged to `main` — the branch model
CLAUDE.md rev 5 itself now states).
