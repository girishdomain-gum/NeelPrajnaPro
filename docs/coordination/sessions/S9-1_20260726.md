# S9-1 · session log · 2026-07-26 · developer (claude-code)
Instruction: ARCH-009 (Sprint 9 — Second Lens, Rebuild, Enforcement).
Branch: claude/arch-009-t0-execution-59fec3 · Pushed through: commit `4381501`
(T0 + §1 + §2). Boot per CLAUDE.md: `git fetch` OK, `origin/main` already merged
(branched off f3201ce, the Owner's ARCH-009 data drop). Read PROTOCOL, state file
(stale, pre-S3 — noted), latest session log (S8-2), ARCH-009 FINAL + its two
addenda, GO-S8, DEVQ-018/020, the data-drop provenance sidecars.

## DONE (committed + pushed)

**T0 — `scripts/t0_s9.py`, commit af2900e.** Appended the GO-S8 `note` record
(`01KYDA4KXFBAA7461JJP6H1ZX9`), parented on the GO-S7 note S8's T0 laid down
(`01KYCNVX45TCMS16D22T2NEP3H`). Records the Owner GO ("GO. I agree with closing
Sprint 8."), the Wave-1 hypothesis/placebo/verdict/burn/belief ids, and marks S9
open per ARCH-009. Idempotent. Journal 54→55, chain GREEN.

**§1 — rebuild-bulk for verdict_trades, commit f1350d3.**
- Refactor: extracted `EvidenceBattery.trades_table(outcomes)` (static) as the
  SINGLE construction of the `verdict_trades.*` table; `_trades_manifest` (write
  path) and the new rebuild path both call it, so a rebuild is byte-identical BY
  CONSTRUCTION (never a parallel implementation — the §1 binding).
- `scripts/rebuild_bulk.py`: for every verdict with a `trades_manifest`,
  reconstructs the exact setup events per lineage (dispatch: h001 = SMCFVGDetector;
  h002 = intra-week filter; h003 = Monday markers — the SAME builders the judges
  used, loaded by file-path since scripts/ is not a package), re-runs the recorded
  experiment via `EvidenceBattery.evaluate` (the placebo/replay entry — no burn),
  rebuilds the table, writes it to the manifest's path, and ASSERTS rebuilt
  sha256 == manifest.file_sha256 (loud SystemExit on any mismatch; a rebuild that
  "mostly matches" is a fabrication). Bars parquet rebuilt first (reuses
  judge_h001). An unregistered lineage is a loud failure, never a silent skip.
- Proven on all three on a clean bulk root: h001 (654 rows) / h002 (637) /
  h003 (28) all sha assert-equal. Appends nothing (asserted).
- Tests `tests/scripts/test_rebuild_bulk_s9.py`: matches-recorded-manifests;
  refuses-unknown-lineage; **byte-stable across process restarts** (two fresh
  interpreters both reproduce the recorded manifests). 3 passed.

**§2 — placebo_method under the seal, commit 4381501.**
- Optional v2 hypothesis field `placebo_method` (schema `_validate_hypothesis_v2`,
  additive → Wave-1 H-002/H-003 grandfather unchanged, NO migration). Registry
  `_resolve_placebo_method` validates membership against `placebo.PLACEBO_METHODS`
  (the judge's OWN dispatch set — one source of truth) and REFUSES an unknown
  method; the content-hash seal covers it (verify_frozen catches a method edit);
  it requires a v2 hypothesis (like observatory_ancestry).
- `PlaceboBattery.run` REFUSES a requested method that disagrees with the sealed
  `placebo_method` (SchemaViolation naming both); a grandfathered record (no
  sealed field) runs with the caller's method exactly as Wave-1 did.
- Tests: `tests/protocol/test_hypotheses_placebo_method_s9.py` (seal, unknown
  refusal, requires-v2, verify_frozen tamper, grandfather shape) +
  `tests/battery/test_placebo_method_seal_s9.py` (judge mismatch/agree/grandfather).
  9 passed.

Regression: battery + protocol + records + graduation + firewall suites GREEN;
ruff clean on all changed files; journal chain GREEN (55). VIRGIN untouched.

## IN-PROGRESS / BLOCKED — §4 (the independent lens)

§4 is scoped and understood but has a **hard Owner-gate + an ordering coupling**
that a non-interactive Developer session cannot cross without the Owner:

1. **VIRGIN designation is Owner-only** (A1.2 "VIRGIN behind typed phrases only";
   `declare_virgin_s3.py` reads the exact phrase `DECLARE VIRGIN` from an
   interactive console — "the Owner runs this, not the AI"). §4.2 requires a NEW
   VIRGIN slice designated from the 2025 extension. I must not fabricate that
   authorization.
2. **Ordering coupling.** The §4 ADDENDUM binds the overlap/agreement to the
   TRAINING-window timespan ONLY, EXCLUDING every VIRGIN window (2024's reserve
   AND the new 2025-extension reserve). The 2025 VIRGIN boundary is exactly the
   Owner-gated act in (1). So §4.1's overlap and §4.3's H-004 (judged on the
   settled 2024+2025 TRAINING) both depend on the Owner setting that boundary
   first. Computing the overlap now would compute over data destined to become
   reserve — a reserve violation.
3. **Consequential pre-registration.** §4.1 requires the agreement threshold +
   OHLC tolerance to be pre-registered in a `note` BEFORE the overlap is computed
   (DEVQ-020 binding). The two feeds differ by broker AND price precision
   (Winprofx 2-digit server-time vs Exness 3-digit UTC), so the tolerance choice
   is scientifically load-bearing and, per protocol, should not be guessed.

Data confirmed present + adapter-compatible: both drop CSVs use
`time_open_sec,time_close_sec,open,high,low,close,rsi14,dow` (reuses
`IVF_S2_COLUMN_MAP`; ts = close basis, OBS-4). Provenance sidecars read: second
feed declared_independence_tier=**broker** (Exness-MT5Trial7 XAUUSDm, separate
from Winprofx, upstream LP unknown). `second_lens` schema exists (closed-key:
{source_name, overlap_manifest, agreement_summary{n_overlap,n_agree,
agreement_rate,notes}}) — tier must live in source_name text + the ingest note,
clock-era tables in agreement_summary.notes (schema is frozen; not modified).

## BLOCKED-ON: Owner direction on the §4 VIRGIN gate + threshold pre-registration
(see the two questions raised to the Owner this session). §3 is Architect-owned
(no Developer action). NEXT: on Owner direction — pre-register the note, ingest
both feeds (raw storage is not computation), build the clock-era alignment +
overlap engine + the 2025 TRAINING/VIRGIN split tool (Owner runs the typed
phrase), then §4.1 second_lens + §4.3 H-004.

Tests: 795 passed (786 prior + 3 §1 + 9 §2 − 3 counting overlap in shared files;
exact full-suite count to be confirmed at close) · ruff clean · journal 55, chain
GREEN · VIRGIN untouched · no writes outside permitted paths (scripts/, qrf/
production + tests, journal via RecordStore.append only, this session log).
